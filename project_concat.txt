--- Concatenation du projet : C:\Users\aurel\Documents\full of doc\POW'AIR\html version\toufff one ---
--- Généré le : 2025-05-13 17:04:01.786922 ---

--- Fichiers Inclus (3) ---
- index.html
- lading_partner.html
- style.css

========================================

--- Début du fichier : index.html ---

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>POW'AIR - Ne tombez plus jamais à court de batterie</title>

  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">

  <link rel="stylesheet" href="style.css">
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
     integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY="
     crossorigin=""/>
  </head>
<body>
  <div class="gradient-bg">
    <div class="gradient-orb"></div>
    <div class="gradient-orb"></div>
    <div class="gradient-orb"></div>
  </div>

  <div class="mode-switch">
    <i class="fas fa-moon"></i>
  </div>

  <header>
  <div class="container">
  <div class="header-content">
  <a href="#" class="logo">
  <img src="images/logo-powair.png" alt="Pow'air Logo Icon"> Pow<span>'air</span>
  </a>
  <nav>
  <ul>
  <li><a href="#how-it-works">Comment ça marche</a></li>
  <li><a href="#network">Notre réseau</a></li>
  <li><a href="#benefits">Avantages</a></li>
  <li><a href="#pricing">Tarifs</a></li>
  <li><a href="#partners">Partenaires</a></li>
  </ul>
  <a href="#download" class="btn btn-primary">
  <i class="fas fa-download"></i>
  Télécharger l'App
  </a>
  <button class="mobile-menu-button" aria-label="Ouvrir le menu">
  <i class="fas fa-bars"></i>
  </button>
  </nav>
  </div>
  </div>
  <div class="mobile-menu">
  <ul>
  <li style="--i:1"><a href="#how-it-works">Comment ça marche</a></li>
  <li style="--i:2"><a href="#network">Notre réseau</a></li>
  <li style="--i:3"><a href="#benefits">Avantages</a></li>
  <li style="--i:4"><a href="#pricing">Tarifs</a></li>
  <li style="--i:5"><a href="#partners">Partenaires</a></li>
  <li style="--i:6"><a href="#download" class="btn btn-primary">Télécharger l'App</a></li>
  </ul>
  </div>
 </header>

  <section class="hero" id="hero">
  <div class="container">
    <div class="hero-container" style="position: relative; background: linear-gradient(135deg, var(--bg-alt), #E0F2FE); border-radius: var(--radius-xl); overflow: hidden; padding: 3rem 2rem; box-shadow: var(--shadow-lg); border: 1px solid var(--border-color);">
      <div class="hero-content">
        <div class="hero-text">
          <div class="hero-badge">
            <i class="fas fa-check-circle"></i>
            #1 en France pour la recharge mobile
          </div>
          <h1>Louez, <span>Chargez, </span>Retournez ! </h1>
          <p>
            Louez une batterie externe dans l’une de nos bornes, utilisez-la aussi longtemps que nécessaire, et retournez la dans la borne la plus proche !
          </p>
        </div>
        <div class="hero-image">
          <div class="hero-phone-container">
              <div class="hero-phone no-frame">
                <img src="https://i.imgur.com/s5HERPN.png" alt="Mockup Pow'air" class="hero-phone-screen" loading="lazy">
              </div>
          </div>
        </div>
        <div class="hero-buttons">
          <a href="#download" class="btn btn-primary">
            <i class="fas fa-download"></i>
            Télécharger l'App
          </a>
          <a href="#network" class="btn btn-secondary">
            <i class="fas fa-map-marker-alt"></i>
            Trouver une borne
          </a>
        </div>
      </div>
      <div class="scroll-down">
        <div class="scroll-down-text">Découvrir</div>
        <div class="scroll-down-arrow">
          <i class="fas fa-chevron-down"></i>
        </div>
      </div>
    </div>
  </div>
</section>

  <section class="how-it-works" id="how-it-works">
    <div class="section-bg">
      <div class="section-bg-pattern"></div>
    </div>
    <div class="container">
      <div class="section-inner">
        <div class="section-title-container">
          <div class="section-tag">
            <i class="fas fa-info-circle"></i> Simple et rapide
          </div>
          <h2>Comment ça marche</h2>
          <p class="subtitle">Un service qui vous libère de toutes contraintes de batterie.</p>
        </div>
        <div class="steps">
          <div class="step">
            <div class="step-number">1</div>
            <div class="step-illustration">
                <img src="https://i.imgur.com/OxDKSEW.png" alt="Illustration Étape 1: Scanner le QR code sur la borne Pow'air">
            </div>
            <h3 class="step-title">Scannez</h3>
            <p class="step-description">Localisez une borne via l’application et scannez le QR code pour déverrouiller votre batterie.</p>
          </div>
          <div class="step">
            <div class="step-number">2</div>
            <div class="step-illustration">
              <img src="https://i.imgur.com/I7pNgA5.png" alt="Illustration Étape 2: Utiliser la batterie Pow'air pour charger un téléphone">
            </div>
            <h3 class="step-title">Utilisez</h3>
            <p class="step-description">Emportez votre batterie partout avec vous et rechargez vos appareils à volonté, aussi longtemps que nécessaire.</p>
          </div>
          <div class="step">
            <div class="step-number">3</div>
              <div class="step-illustration">
                <img src="https://i.imgur.com/1zPlHgH.png" alt="Illustration Étape 3: Retourner la batterie Pow'air dans une borne">
            </div>
            <h3 class="step-title">Retournez</h3>
            <p class="step-description">Retournez la batterie dans la borne la plus proche !</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="network" id="network">
    <div class="section-bg">
      <div class="section-bg-pattern"></div>
    </div>
    <div class="container">
      <div class="section-inner">
        <div class="section-title-container">
          <div class="section-tag">
            <i class="fas fa-network-wired"></i> Notre Réseau
          </div>
          <h2>Des milliers de points près de chez vous</h2>
          <p class="subtitle">Localisez la borne POW'AIR la plus proche en un clin d'œil grâce à notre carte interactive.</p>
        </div>
        <div class="map-container">
          <div class="map-wrapper">
            <div class="map-search-container">
              <div class="map-search-wrapper">
                <input type="text" id="city-search" placeholder="Rechercher une ville..." class="map-search-input" autocomplete="off" inputmode="search">
                <button id="search-button" class="btn btn-primary map-search-button">
                  <i class="fas fa-search"></i>
                </button>
                <div id="search-suggestions" class="search-suggestions"></div>
              </div>
            </div>
            <div id="map"></div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="benefits" id="benefits">
    <div class="benefits-bg">
      <div class="benefits-bg-shape"></div>
      <div class="benefits-bg-shape"></div>
    </div>
    <div class="container">
      <div class="section-inner">
        <div class="section-title-container">
          <div class="section-tag">
            <i class="fas fa-star"></i> Vos avantages
          </div>
          <h2>Pourquoi choisir POW'AIR ?</h2>
          <p class="subtitle">Découvrez ce qui rend notre service unique et indispensable au quotidien.</p>
        </div>
        <div class="benefits-list">
          <div class="benefit-card">
            <div class="benefit-content">
              <div class="benefit-icon"><i class="fas fa-bolt"></i></div>
              <h3 class="benefit-title">Recharge ultra-rapide</h3>
              <p class="benefit-description">Nos batteries intègrent les dernières technologies pour recharger vos appareils en un temps record.</p>
            </div>
          </div>
          <div class="benefit-card">
            <div class="benefit-content">
              <div class="benefit-icon"><i class="fas fa-map-marked-alt"></i></div>
              <h3 class="benefit-title">Réseau étendu</h3>
              <p class="benefit-description">Trouvez et retournez une batterie POW'AIR facilement grâce à nos milliers de points partenaires.</p>
            </div>
          </div>
          <div class="benefit-card">
            <div class="benefit-content">
              <div class="benefit-icon"><i class="fas fa-wallet"></i></div>
              <h3 class="benefit-title">Tarifs flexibles</h3>
              <p class="benefit-description">Payez uniquement ce que vous consommez ou optez pour nos abonnements avantageux.</p>
            </div>
          </div>
          </div>
      </div>
    </div>
  </section>

  <section class="pricing" id="pricing">
    <div class="container">
      <div class="section-inner">
        <div class="section-title-container">
          <div class="section-tag">
            <i class="fas fa-tags"></i> Nos Tarifs
          </div>
          <h2>Choisissez la formule qui vous convient</h2>
          <p class="subtitle">Des options adaptées à vos besoins.</p>
        </div>
        <div class="pricing-options">
          <div class="pricing-card">
            <div class="pricing-header">
              <h3 class="pricing-name">Flex</h3>
              <div class="pricing-price">0.99€</div>
              <span class="pricing-period">par heure</span>
            </div>
            <p class="pricing-description">Payez à l'heure, avec les 20 premières minutes offertes.</p>
            <ul class="pricing-features">
              <li class="pricing-feature"><i class="fas fa-check"></i><span class="pricing-feature-text">Première 1/2h gratuite</span></li>
              <li class="pricing-feature"><i class="fas fa-check"></i><span class="pricing-feature-text">Payez par heure utilisée</span></li>
              <li class="pricing-feature"><i class="fas fa-check"></i><span class="pricing-feature-text">Pas d'engagement</span></li>
              <li class="pricing-feature"><i class="fas fa-check"></i><span class="pricing-feature-text">Accès à tout le réseau</span></li>
            </ul>
            <div class="pricing-action">
              <a href="#download" class="btn btn-secondary">Commencer</a>
            </div>
          </div>
          <div class="pricing-card popular">
            <div class="popular-badge">Populaire</div>
            <div class="pricing-header">
              <h3 class="pricing-name">Pass 24h</h3>
              <div class="pricing-price">3.99€</div>
              <span class="pricing-period">par jour</span>
            </div>
              <p class="pricing-description">Idéal pour une journée chargée ou un événement.</p>
            <ul class="pricing-features">
              <li class="pricing-feature"><i class="fas fa-check"></i><span class="pricing-feature-text">Utilisation illimitée pendant 24h</span></li>
              <li class="pricing-feature"><i class="fas fa-check"></i><span class="pricing-feature-text">Retour flexible</span></li>
              <li class="pricing-feature"><i class="fas fa-check"></i><span class="pricing-feature-text">Valable 24h après activation</span></li>
              <li class="pricing-feature"><i class="fas fa-check"></i><span class="pricing-feature-text">Accès à tout le réseau</span></li>
            </ul>
            <div class="pricing-action">
              <a href="#download" class="btn btn-primary">Choisir cette offre</a>
            </div>
          </div>
          <div class="pricing-card">
            <div class="pricing-header">
              <h3 class="pricing-name">Illimité</h3>
              <div class="pricing-price">9.99€</div>
              <span class="pricing-period">par mois</span>
            </div>
                <p class="pricing-description">La tranquillité d'esprit, rechargez sans compter.</p>
            <ul class="pricing-features">
              <li class="pricing-feature"><i class="fas fa-check"></i><span class="pricing-feature-text">Recharges illimitées</span></li>
              <li class="pricing-feature"><i class="fas fa-check"></i><span class="pricing-feature-text">Facturation mensuelle unique</span></li>
              <li class="pricing-feature"><i class="fas fa-check"></i><span class="pricing-feature-text">Sans engagement</span></li>
              <li class="pricing-feature"><i class="fas fa-check"></i><span class="pricing-feature-text">Support prioritaire</span></li>
                <li class="pricing-feature"><i class="fas fa-check"></i><span class="pricing-feature-text">Accès à tout le réseau</span></li>
            </ul>
            <div class="pricing-action">
              <a href="#download" class="btn btn-secondary">S'abonner</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="partners" id="partners">
    <div class="partners-bg">
      <div class="partners-bg-shape"></div>
      <div class="partners-bg-shape"></div>
    </div>
    <div class="container">
      <div class="section-inner">
        <div class="section-title-container">
          <div class="section-tag partner-badge">
            <i class="fas fa-handshake"></i> Devenez Partenaire
          </div>
          <h2>Rejoignez l'aventure POW'AIR</h2>
          <p class="subtitle">Développons ensemble le réseau POW’AIR.</p>
        </div>
        <div class="partners-benefits">
          <h3 class="partners-subtitle" style="text-align: center;">Pourquoi rejoindre notre réseau ?</h3>
          <div class="partners-cards">
            <div class="partner-card">
              <div class="partner-card-icon"><i class="fas fa-users"></i></div>
              <div class="partner-card-content">
                <h4>Augmentez votre trafic</h4>
                <p>Attirez de nouveaux clients grâce à notre app qui dirige les utilisateurs vers votre établissement pour recharger leur appareil.</p>
              </div>
              <div class="partner-card-bg"></div>
            </div>
            <div class="partner-card">
              <div class="partner-card-icon"><i class="fas fa-euro-sign"></i></div>
              <div class="partner-card-content">
                <h4>Revenus additionnels</h4>
                <p>Gagnez une commission sur chaque location et retour de batterie, créant un flux de revenus supplémentaire sans effort.</p>
              </div>
              <div class="partner-card-bg"></div>
            </div>
            <div class="partner-card">
              <div class="partner-card-icon"><i class="fas fa-bullhorn"></i></div>
              <div class="partner-card-content">
                <h4>Visibilité accrue</h4>
                <p>Votre établissement sera mis en avant sur notre application et dans nos campagnes marketing auprès de milliers d'utilisateurs.</p>
              </div>
              <div class="partner-card-bg"></div>
            </div>
            <div class="partner-card">
              <div class="partner-card-icon"><i class="fas fa-concierge-bell"></i></div>
              <div class="partner-card-content">
                <h4>Service différenciant</h4>
                <p>Offrez à vos clients un service moderne, utile et éco-responsable qui valorise votre établissement.</p>
              </div>
              <div class="partner-card-bg"></div>
            </div>
          </div>
        </div>
        <div class="partner-toggle-container">
          <a href="lading_partner.html" class="btn btn-secondary partner-toggle-button">
              <i class="fas fa-external-link-alt"></i> En savoir plus sur le partenariat
          </a>
      </div>
        <div class="partner-hidden-content">
            <div class="partners-stats">
              <div class="stat-item">
                <div class="stat-number" data-count="2500">0</div>
                <div class="stat-label">Partenaires</div>
              </div>
              <div class="stat-item">
                <div class="stat-number" data-count="35">0</div>
                <div class="stat-label">Villes couvertes</div>
              </div>
              <div class="stat-item">
                <div class="stat-number" data-count="98">0</div>
                <div class="stat-label">% de satisfaction</div>
              </div>
            </div>
            <div class="partners-testimonials">
                <h3 class="partners-subtitle" style="text-align: center;">Ils nous font confiance</h3>
                <div class="testimonial-slider">
                    <div class="testimonial-slide active">
                      <div class="testimonial-content">
                        <p>"Depuis que nous sommes partenaires POW'AIR, nous avons constaté une augmentation de 15% de notre clientèle, avec des clients plus fidèles qui reviennent régulièrement."</p>
                        <div class="testimonial-author">
                          <div class="testimonial-avatar"><i class="fas fa-store"></i></div>
                          <div class="testimonial-info">
                            <h5>Café des Arts</h5>
                            <span>Partenaire depuis 2022</span>
                          </div>
                        </div>
                      </div>
                    </div>
                    <div class="testimonial-slide">
                        <div class="testimonial-content">
                          <p>"Le service client POW'AIR est exceptionnel. L'installation a été rapide et nous générons maintenant des revenus complémentaires tout en offrant un service utile."</p>
                          <div class="testimonial-author">
                            <div class="testimonial-avatar"><i class="fas fa-shopping-bag"></i></div>
                            <div class="testimonial-info">
                              <h5>Librairie Page & Plume</h5>
                              <span>Partenaire depuis 2023</span>
                            </div>
                          </div>
                        </div>
                    </div>
                    <div class="testimonial-slide">
                        <div class="testimonial-content">
                          <p>"Nos clients adorent pouvoir recharger leurs appareils pendant qu'ils font leurs courses. C'est un vrai plus qui nous différencie de nos concurrents."</p>
                          <div class="testimonial-author">
                            <div class="testimonial-avatar"><i class="fas fa-utensils"></i></div>
                            <div class="testimonial-info">
                              <h5>Restaurant Le Gourmet</h5>
                              <span>Partenaire depuis 2021</span>
                            </div>
                          </div>
                        </div>
                    </div>
                  <div class="testimonial-dots">
                    <span class="dot active" data-slide="0"></span>
                    <span class="dot" data-slide="1"></span>
                    <span class="dot" data-slide="2"></span>
                  </div>
                </div>
            </div>
            <div class="partner-process">
                <h3 class="partners-subtitle" style="text-align: center;">Comment ça marche ?</h3>
                <div class="process-steps">
                    <div class="process-step">
                      <div class="process-step-number">1</div>
                      <div class="process-step-content">
                        <h4>Contactez-nous</h4>
                        <p>Remplissez le formulaire ou appelez-nous pour discuter de votre intérêt</p>
                      </div>
                    </div>
                    <div class="process-step">
                      <div class="process-step-number">2</div>
                      <div class="process-step-content">
                        <h4>Installation gratuite</h4>
                        <p>Nous installons le matériel sans frais dans votre établissement</p>
                      </div>
                    </div>
                    <div class="process-step">
                      <div class="process-step-number">3</div>
                      <div class="process-step-content">
                        <h4>Profitez des avantages</h4>
                        <p>Commencez à générer du trafic et des revenus additionnels</p>
                      </div>
                    </div>
                </div>
            </div>
            <div class="partner-contact-form">
              <form id="partner-form">
                <div class="form-header">
                  <h3>Prêt à nous rejoindre ?</h3>
                  <p>Laissez-nous vos coordonnées et un conseiller vous contactera sous 24h.</p>
                </div>
                <div class="form-content">
                  <div class="form-fields">
                    <div class="form-field">
                      <input type="text" placeholder="Nom de l'établissement" name="establishmentName" required>
                    </div>
                    <div class="form-field">
                      <input type="email" placeholder="Email professionnel" name="email" required>
                    </div>
                    <div class="form-field">
                      <input type="tel" placeholder="Téléphone" name="phone" required>
                    </div>
                    <div class="form-field">
                      <select name="establishmentType" required>
                        <option value="" disabled selected>Type d'établissement</option>
                        <option value="Café / Restaurant">Café / Restaurant</option>
                        <option value="Hôtel">Hôtel</option>
                        <option value="Commerce">Commerce</option>
                        <option value="Autre">Autre</option>
                      </select>
                    </div>
                  </div>
                  <button type="submit" class="btn btn-primary partner-submit" id="partner-submit-button">
                    <i class="fas fa-paper-plane"></i>
                    Devenir Partenaire
                  </button>
                  <div id="partner-form-message" style="margin-top: 1rem; text-align: center; min-height: 1.5em; color: inherit;"></div>
                </div>
                <div class="form-alternative">
                  <span>Ou appelez-nous directement au </span>
                  <a href="tel:+33123456789" class="phone-link">01 23 45 67 89</a>
                </div>
              </form>
            </div>
            <div class="partner-brands">
                <h4>Ils font déjà partie de l'aventure</h4>
                <div class="partner-logos">
                  <div class="partner-logo"><i class="fas fa-coffee"></i> Café Express</div>
                  <div class="partner-logo"><i class="fas fa-hotel"></i> Hôtels Élégance</div>
                  <div class="partner-logo"><i class="fas fa-shopping-cart"></i> SuperMarché</div>
                  <div class="partner-logo"><i class="fas fa-book"></i> Médiathèques</div>
                  <div class="partner-logo"><i class="fas fa-utensils"></i> RestoPrime</div>
                </div>
            </div>
        </div>
      </div>
    </div>
  </section>

  <section class="download" id="download">
    <div class="container">
      <div class="section-inner">
        <div class="download-container">
          <div class="download-bg"></div>
          <div class="download-content">
            <div class="download-text">
              <h2 class="download-title">Téléchargez l'application POW'AIR</h2>
              <p class="download-description">
                Accédez à notre réseau, gérez votre compte et rechargez votre mobile où que vous soyez. Disponible sur iOS et Android.
              </p>
              <div class="download-buttons">
                <a href="#" class="app-button" target="_blank" rel="noopener noreferrer">
                  <i class="fab fa-apple app-button-icon"></i>
                  <div class="app-button-text">
                    <span class="app-button-label">Télécharger sur</span>
                    <span class="app-button-store">App Store</span>
                  </div>
                </a>
                <a href="#" class="app-button" target="_blank" rel="noopener noreferrer">
                  <i class="fab fa-google-play app-button-icon"></i>
                  <div class="app-button-text">
                    <span class="app-button-label">Disponible sur</span>
                    <span class="app-button-store">Google Play</span>
                  </div>
                </a>
              </div>
            </div>
            <div class="download-app-preview">
              <div class="app-preview-phone">
                <div class="app-preview-phone-frame">
                  <img src="https://i.imgur.com/kC04xF1.png" alt="Aperçu de l'application Pow'air" class="app-preview-screen" loading="lazy">
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>


  <footer id="footer">
  <div class="footer-bg"></div>
  <div class="container">
  <div class="footer-content">
  <div class="footer-logo-area">
  <a href="#" class="footer-logo">
  <img src="images/logo-powair.png" alt="Pow'air Logo Icon"> Pow<span>'air</span>
  </a>
  <p class="footer-description">
  La solution simple et rapide pour ne plus jamais être à court de batterie. Louez, utilisez, retournez.
  </p>
  <div class="footer-social">
  <a href="#" class="social-icon" aria-label="Facebook" target="_blank" rel="noopener noreferrer"><i class="fab fa-facebook-f"></i></a>
  <a href="#" class="social-icon" aria-label="Instagram" target="_blank" rel="noopener noreferrer"><i class="fab fa-instagram"></i></a>
  <a href="#" class="social-icon" aria-label="Twitter" target="_blank" rel="noopener noreferrer"><i class="fab fa-twitter"></i></a>
  <a href="#" class="social-icon" aria-label="LinkedIn" target="_blank" rel="noopener noreferrer"><i class="fab fa-linkedin-in"></i></a>
  </div>
  </div>
  <div>
  <h4 class="footer-title">Liens rapides</h4>
  <ul class="footer-links">
  <li><a href="#how-it-works">Comment ça marche</a></li>
  <li><a href="#network">Notre réseau</a></li>
  <li><a href="#benefits">Avantages</a></li>
  <li><a href="#pricing">Tarifs</a></li>
  <li><a href="#partners">Devenir partenaire</a></li>
  </ul>
  </div>
  <div>
  <h4 class="footer-title">Légal</h4>
  <ul class="footer-links">
  <li><a href="#">Conditions générales</a></li>
  <li><a href="#">Politique de confidentialité</a></li>
  <li><a href="#">Mentions légales</a></li>
  <li><a href="#">Gestion des cookies</a></li>
  </ul>
  </div>
  <div>
  <h4 class="footer-title">Contactez-nous</h4>
  <div class="footer-contact">
  <div class="contact-item">
  <div class="contact-icon"><i class="fas fa-map-marker-alt"></i></div>
  <span class="contact-text">123 Rue Fictive,<br>75000 Paris, France</span>
  </div>
  <div class="contact-item">
  <div class="contact-icon"><i class="fas fa-envelope"></i></div>
  <span class="contact-text"><a href="mailto:contact@powair.example.com">contact@powair.example.com</a></span>
  </div>
  <div class="contact-item">
  <div class="contact-icon"><i class="fas fa-phone"></i></div>
  <span class="contact-text"><a href="tel:+33123456789">+33 1 23 45 67 89</a></span>
  </div>
  </div>
  </div>
  </div>
  <div class="footer-divider"></div>
  <div class="footer-bottom">
  <p class="footer-copyright">
  © 2024 POW'AIR. Tous droits réservés. Développé avec <i class="fas fa-heart"></i>.
  </p>
  <div class="footer-links-bottom">
  <a href="#">Conditions</a>
  <a href="#">Confidentialité</a>
  <a href="#">Plan du site</a>
  </div>
  </div>
  </div>
 </footer>

  <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"
     integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo="
     crossorigin=""></script>
  <script src="script.js"></script>

</body>
</html>

--- Fin du fichier : index.html ---

--- Début du fichier : lading_partner.html ---

<!DOCTYPE html>
<html lang="fr">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pow'air Partenaires - Rejoignez le réseau de recharge mobile #1 en France</title>

  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">

  <link rel="stylesheet" href="style.css">

  <style>
    /* Ajustements spécifiques pour la landing page commerçants */

    .metric-card {
      padding: 2rem;
      border-radius: var(--radius-lg);
      background-color: var(--bg-color);
      box-shadow: var(--shadow);
      border: 1px solid var(--border-color);
      text-align: center;
      transition: all 0.3s ease;
    }

    .metric-card:hover {
      transform: translateY(-10px);
      box-shadow: var(--shadow-lg);
      border-color: var(--accent-color);
    }

    .metric-icon {
      font-size: 2.5rem;
      color: var(--accent-color);
      margin-bottom: 1rem;
    }

    .metric-number {
      font-size: 3rem;
      font-weight: 800;
      color: var(--accent-color);
      line-height: 1;
      margin-bottom: 0.5rem;
    }

    .metric-label {
      color: var(--text-light);
      font-size: 1.1rem;
      font-weight: 500;
    }

    .metrics-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 1.5rem;
      margin-top: 3rem;
    }

    .feature-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
      margin-top: 3rem;
    }

    .feature-card {
      padding: 2rem;
      border-radius: var(--radius-lg);
      background-color: var(--bg-color);
      box-shadow: var(--shadow);
      border: 1px solid var(--border-color);
      transition: all 0.3s ease;
      height: 100%;
      display: flex;
      flex-direction: column;
    }

    .feature-card:hover {
      transform: translateY(-10px);
      box-shadow: var(--shadow-lg);
      border-color: var(--accent-color);
    }

    .feature-icon {
      font-size: 2rem;
      color: var(--accent-color);
      margin-bottom: 1rem;
      background-image: var(--gradient-primary);
      background-color: var(--gradient-start-color);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .feature-title {
      font-size: 1.5rem;
      font-weight: 700;
      margin-bottom: 1rem;
      color: var(--text-color);
    }

    .feature-description {
      color: var(--text-light);
      font-size: 1rem;
      line-height: 1.6;
      margin-bottom: 0;
      flex-grow: 1;
    }

    .hero-merchant {
      position: relative;
      background: linear-gradient(135deg, var(--bg-alt), #E0F2FE);
      border-radius: var(--radius-xl);
      overflow: hidden;
      padding: 3rem 2rem;
      margin-top: 1,5rem;
      box-shadow: var(--shadow-lg);
      border: 1px solid var(--border-color);
    }

    .dark-mode .hero-merchant {
      background: linear-gradient(135deg, var(--bg-alt-dark), var(--bg-color-dark));
    }

    /* Styles pour les témoignages (même si la section est enlevée, on peut laisser les styles au cas où) */
    .testimonial-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
      margin-top: 3rem;
    }

    .testimonial-card {
      padding: 2rem;
      border-radius: var(--radius-lg);
      background-color: var(--bg-color);
      box-shadow: var(--shadow);
      border: 1px solid var(--border-color);
      transition: all 0.3s ease;
      height: 100%;
    }

    .testimonial-card:hover {
      transform: translateY(-10px);
      box-shadow: var(--shadow-lg);
      border-color: var(--accent-color);
    }

    .testimonial-quote {
      font-style: italic;
      color: var(--text-color);
      position: relative;
      padding: 0 1rem;
      margin-bottom: 1.5rem;
    }

    .testimonial-quote::before,
    .testimonial-quote::after {
      content: '"';
      font-size: 2rem;
      font-family: 'Georgia', serif;
      line-height: 0;
      color: var(--accent-color);
      opacity: 0.5;
      position: absolute;
    }

    .testimonial-quote::before {
      left: 0;
      top: 0.5rem;
    }

    .testimonial-quote::after {
      right: 0;
      bottom: -0.3rem;
    }

    .testimonial-author-info {
      display: flex;
      align-items: center;
    }

    .testimonial-avatar {
      width: 3rem;
      height: 3rem;
      background-image: var(--gradient-primary);
      background-color: var(--gradient-start-color);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-right: 1rem;
      color: var(--main-white);
      font-size: 1.2rem;
    }

    .testimonial-author h5 {
      font-size: 1.1rem;
      font-weight: 700;
      margin-bottom: 0.2rem;
      color: var(--text-color);
    }

    .testimonial-author span {
      font-size: 0.85rem;
      color: var(--text-light);
    }
    /* Fin des styles témoignages */


    .faq-container {
      margin-top: 3rem;
    }

    .faq-item {
      margin-bottom: 1.5rem;
      border-radius: var(--radius-lg);
      background-color: var(--bg-color);
      box-shadow: var(--shadow);
      border: 1px solid var(--border-color);
      overflow: hidden;
      transition: all 0.3s ease;
    }

    .faq-item:hover {
      box-shadow: var(--shadow-md);
      border-color: var(--accent-color);
    }

    .faq-question {
      padding: 1.5rem;
      font-weight: 700;
      font-size: 1.1rem;
      color: var(--text-color);
      display: flex;
      justify-content: space-between;
      align-items: center;
      cursor: pointer;
    }

    .faq-question i {
      color: var(--accent-color);
      transition: transform 0.3s ease;
    }

    .faq-question.active i {
      transform: rotate(180deg);
    }

    .faq-answer {
      padding: 0 1.5rem 1.5rem;
      color: var(--text-light);
      font-size: 1rem;
      line-height: 1.6;
      display: none;
    }

    .faq-answer.active {
      display: block;
      animation: fadeIn 0.5s ease;
    }

    .hero-cta {
      margin-top: 2rem;
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
    }

    .comparison-table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 3rem;
      border-radius: var(--radius-lg);
      overflow: hidden;
      box-shadow: var(--shadow-lg);
    }

    .comparison-table th,
    .comparison-table td {
      padding: 1.2rem;
      text-align: center;
      border-bottom: 1px solid var(--border-color);
    }

    .comparison-table th {
      background-color: var(--accent-color);
      color: var(--main-white);
      font-weight: 700;
      font-size: 1.1rem;
    }

    /* Ajustement : on cible spécifiquement le 2ème th pour le centrage */
    .comparison-table th:nth-child(2) {
        text-align: center;
    }

    .comparison-table tbody tr {
      background-color: var(--bg-color);
      transition: all 0.3s ease;
    }

    .comparison-table tbody tr:hover {
      background-color: var(--bg-alt);
    }

    .comparison-table td:first-child {
      text-align: left;
      font-weight: 600;
      color: var(--text-color);
    }

    /* Ajustement : on cible spécifiquement le 2ème td pour le centrage */
     .comparison-table td:nth-child(2) {
       text-align: center;
     }

    .comparison-table .check {
      color: #22c55e;
      font-size: 1.2rem;
    }

    /* La classe .times n'est plus utilisée mais on la garde au cas où */
    .comparison-table .times {
      color: #ef4444;
      font-size: 1.2rem;
    }

    .step-merchant {
      padding: 1.5rem;
      background-color: var(--bg-color);
      border-radius: var(--radius-lg);
      box-shadow: var(--shadow);
      transition: all 0.3s ease;
      border: 1px solid var(--border-color);
      display: flex;
      align-items: center;
      gap: 1.5rem;
      margin-bottom: 1.5rem;
    }

    .step-merchant:hover {
      transform: translateY(-5px);
      box-shadow: var(--shadow-md);
      border-color: var(--accent-color);
    }

    .step-number-merchant {
      min-width: 3rem;
      height: 3rem;
      background-image: var(--gradient-primary);
      background-color: var(--gradient-start-color);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--main-white);
      font-weight: 700;
      font-size: 1.5rem;
      box-shadow: var(--shadow-sm);
    }

    .step-content-merchant h4 {
      margin-bottom: 0.5rem;
      font-size: 1.2rem;
      color: var(--text-color);
    }

    .step-content-merchant p {
      margin-bottom: 0;
      color: var(--text-light);
    }

    /* Réduction de l'espace pour la section hero en version desktop */


    @media (max-width: 768px) {
      .hero-merchant {
        padding: 3rem 1.5rem;
      }

      .hero-cta {
        flex-direction: column;
      }

      .comparison-table th,
      .comparison-table td {
        padding: 0.8rem;
        font-size: 0.9rem;
      }

      .step-merchant {
        flex-direction: column;
        text-align: center;
        padding: 1.5rem;
      }

      .step-content-merchant {
        width: 100%;
      }
    }
  </style>
</head>

<body>
  <div class="gradient-bg">
    <div class="gradient-orb"></div>
    <div class="gradient-orb"></div>
    <div class="gradient-orb"></div>
  </div>

  <div class="mode-switch">
    <i class="fas fa-moon"></i>
  </div>

  <header>
  <div class="container">
  <div class="header-content">
  <a href="index.html" class="logo">
  <img src="images/logo-powair.png" alt="Pow'air Logo Icon"> Pow<span>'air</span>
  </a>
  <nav>
  <ul>
  <li><a href="#pourquoi-partenaire">Pourquoi devenir partenaire</a></li>
  <li><a href="#avantages">Vos avantages</a></li>
  <li><a href="#fonctionnement">Fonctionnement</a></li>
  <li><a href="#faq">FAQ</a></li>
  </ul>
  <a href="#contact" class="btn btn-primary">
  <i class="fas fa-handshake"></i>
  Devenir Partenaire
  </a>
  <button class="mobile-menu-button" aria-label="Ouvrir le menu">
  <i class="fas fa-bars"></i>
  </button>
  </nav>
  </div>
  </div>
  <div class="mobile-menu">
  <ul>
  <li style="--i:1"><a href="#pourquoi-partenaire">Pourquoi devenir partenaire</a></li>
  <li style="--i:2"><a href="#avantages">Vos avantages</a></li>
  <li style="--i:3"><a href="#fonctionnement">Fonctionnement</a></li>
  <li style="--i:4"><a href="#faq">FAQ</a></li>
  <li style="--i:5"><a href="#contact" class="btn btn-primary">Devenir Partenaire</a></li>
  </ul>
  </div>
 </header>

  <section class="hero" id="hero">
    <div class="container">
      <div class="hero-merchant" style="text-align: center; padding: 2.5rem 2rem;">
        <div style="max-width: 800px; margin: 0 auto;">
          <div class="hero-badge" style="display: inline-flex; margin-bottom: 2rem;">
            <i class="fas fa-bolt"></i>
            Solution partenaire #1 pour les commerçants
          </div>

          <h1 style="margin-bottom: 1.5rem;">Boostez votre <span style="color: #4C86F9;">business</span> avec Pow'air</h1>
          <p style="font-size: 1.2rem; color: var(--text-light); margin-bottom: 2.5rem; max-width: 650px; margin-left: auto; margin-right: auto;">
            Rejoignez le réseau de recharge mobile leader en France. Attirez de nouveaux clients, générez des revenus supplémentaires et offrez un service innovant sans investissement.
          </p>

          <div class="hero-cta" style="justify-content: center; margin-top: 2.5rem;">
            <a href="#contact" class="btn btn-primary">
              <i class="fas fa-handshake"></i>
              Devenir Partenaire
            </a>
            <a href="#avantages" class="btn btn-secondary">
              <i class="fas fa-info-circle"></i>
              Découvrir les avantages
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="pourquoi-partenaire" style="padding-top: 40px;">
    <div class="section-bg">
      <div class="section-bg-pattern"></div>
    </div>
    <div class="container">
      <div class="section-inner">
        <div class="section-title-container">
          <div class="section-tag">
            <i class="fas fa-question-circle"></i> Pourquoi rejoindre Pow'air
          </div>
          <h2>Un partenariat gagnant-gagnant</h2>
          <p class="subtitle">Devenez une partie intégrante de l'avenir de la recharge mobile et bénéficiez d'avantages exclusifs.</p>
        </div>

        <div class="feature-grid">
          <div class="feature-card">
            <div class="feature-icon">
              <i class="fas fa-users"></i>
            </div>
            <h3 class="feature-title">Attirez de nouveaux clients</h3>
            <p class="feature-description">Notre application mobile dirige des milliers d'utilisateurs vers les commerces partenaires. Les utilisateurs à la recherche d'une batterie découvrent votre établissement et sont plus susceptibles de revenir.</p>
          </div>

          <div class="feature-card">
            <div class="feature-icon">
              <i class="fas fa-euro-sign"></i>
            </div>
            <h3 class="feature-title">Générez des revenus additionnels</h3>
            <p class="feature-description">Gagnez une commission sur chaque location et retour de batterie dans votre établissement. Un revenu passif sans effort qui s'ajoute à votre chiffre d'affaires principal.</p>
          </div>

          <div class="feature-card">
            <div class="feature-icon">
              <i class="fas fa-bullhorn"></i>
            </div>
            <h3 class="feature-title">Visibilité accrue</h3>
            <p class="feature-description">Votre établissement est mis en avant sur notre application et dans nos campagnes marketing, vous offrant une visibilité auprès de milliers d'utilisateurs potentiels.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="benefits" id="avantages">
    <div class="benefits-bg">
      <div class="benefits-bg-shape"></div>
      <div class="benefits-bg-shape"></div>
    </div>
    <div class="container">
      <div class="section-inner">
        <div class="section-title-container">
          <div class="section-tag">
            <i class="fas fa-gift"></i> Vos avantages exclusifs
          </div>
          <h2>Tout est conçu pour votre réussite</h2>
          <p class="subtitle">Notre programme partenaire offre bien plus qu'une simple borne de recharge.</p>
        </div>

        <table class="comparison-table">
          <thead>
            <tr>
              <th>Avantages</th>
              <th>Partenaires Pow'air</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>Installation et matériel gratuits</td>
              <td><i class="fas fa-check check"></i></td>
            </tr>
            <tr>
              <td>Aucun coût de maintenance</td>
              <td><i class="fas fa-check check"></i></td>
            </tr>
            <tr>
              <td>Commissions sur chaque transaction</td>
              <td><i class="fas fa-check check"></i></td>
            </tr>
            <tr>
              <td>Support technique dédié 7j/7</td>
              <td><i class="fas fa-check check"></i></td>
            </tr>
            <tr>
              <td>Visibilité sur l'application</td>
              <td><i class="fas fa-check check"></i></td>
            </tr>
            <tr>
              <td>Campagnes marketing ciblées</td>
              <td><i class="fas fa-check check"></i></td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </section>

  <section id="fonctionnement" style="padding-bottom: 40px;">
    <div class="section-bg">
      <div class="section-bg-pattern"></div>
    </div>
    <div class="container">
      <div class="section-inner">
        <div class="section-title-container">
          <div class="section-tag">
            <i class="fas fa-cogs"></i> Comment ça fonctionne
          </div>
          <h2>Un processus simple et rapide</h2>
          <p class="subtitle">Devenir partenaire Pow'air est aussi simple que d'utiliser notre service.</p>
        </div>

        <div style="max-width: 800px; margin: 0 auto;">
          <div class="step-merchant">
            <div class="step-number-merchant">1</div>
            <div class="step-content-merchant">
              <h4>Contactez-nous</h4>
              <p>Remplissez le formulaire de contact ou appelez-nous directement. Un conseiller dédié prendra en charge votre demande sous 24h.</p>
            </div>
          </div>

          <div class="step-merchant">
            <div class="step-number-merchant">2</div>
            <div class="step-content-merchant">
              <h4>Installation gratuite</h4>
              <p>Notre équipe installe la borne Pow'air dans votre établissement sans frais et forme votre personnel en moins d'une heure.</p>
            </div>
          </div>

          <div class="step-merchant">
            <div class="step-number-merchant">3</div>
            <div class="step-content-merchant">
              <h4>Activation et lancement</h4>
              <p>Votre point de recharge est immédiatement visible sur l'application et commence à attirer de nouveaux clients dès le premier jour.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="faq" style="padding-top: 40px;"> <div class="section-bg">
      <div class="section-bg-pattern"></div>
    </div>
    <div class="container">
      <div class="section-inner">
        <div class="section-title-container">
          <div class="section-tag">
            <i class="fas fa-question-circle"></i> FAQ
          </div>
          <h2>Vos questions, nos réponses</h2>
          <p class="subtitle">Tout ce que vous devez savoir sur le partenariat avec Pow'air.</p>
        </div>

        <div class="faq-container">
          <div class="faq-item">
            <div class="faq-question">
              Combien coûte l'installation d'une borne Pow'air ?
              <i class="fas fa-chevron-down"></i>
            </div>
            <div class="faq-answer">
              L'installation est entièrement gratuite pour nos partenaires. Nous prenons en charge tous les coûts liés au matériel, à l'installation et à la maintenance des bornes Pow'air.
            </div>
          </div>

          <div class="faq-item">
            <div class="faq-question">
              Comment fonctionne le système de commissions ?
              <i class="fas fa-chevron-down"></i>
            </div>
            <div class="faq-answer">
              Vous recevez une commission sur chaque location initiée dans votre établissement ainsi que sur chaque retour de batterie. Les commissions sont calculées en pourcentage du montant de la transaction et sont versées mensuellement sur votre compte bancaire.
            </div>
          </div>

          <div class="faq-item">
            <div class="faq-question">
              Quelle est la taille d'une borne Pow'air ?
              <i class="fas fa-chevron-down"></i>
            </div>
            <div class="faq-answer">
              Nos bornes sont compactes et s'adaptent à tous les types d'établissements. La taille standard est de 50cm x 30cm x 20cm, mais nous proposons différents modèles selon l'espace disponible dans votre commerce.
            </div>
          </div>

          <div class="faq-item">
            <div class="faq-question">
              Y a-t-il des conditions particulières pour devenir partenaire ?
              <i class="fas fa-chevron-down"></i>
            </div>
            <div class="faq-answer">
              Les principaux critères sont : être un commerce établi depuis au moins 6 mois, avoir un emplacement accessible au public pendant vos heures d'ouverture, et disposer d'une connexion internet (Wi-Fi ou filaire) pour la borne.
            </div>
          </div>

          <div class="faq-item">
            <div class="faq-question">
              Comment se passe la maintenance des bornes ?
              <i class="fas fa-chevron-down"></i>
            </div>
            <div class="faq-answer">
              La maintenance est entièrement prise en charge par Pow'air. En cas de problème technique, notre équipe intervient sous 24 à 48 heures. Les mises à jour du système se font automatiquement à distance. Votre seule responsabilité est de nous signaler rapidement tout dysfonctionnement que vous pourriez constater.
            </div>
          </div>

          <div class="faq-item">
            <div class="faq-question">
              Quelle est la durée d'engagement ?
              <i class="fas fa-chevron-down"></i>
            </div>
            <div class="faq-answer">
              Le contrat de partenariat standard est de 12 mois, renouvelable automatiquement. Vous pouvez toutefois résilier avec un préavis de 2 mois sans aucuns frais. Nous préférons conserver des partenaires satisfaits plutôt que contraints !
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="contact" class="pricing">
    <div class="container">
      <div class="section-inner">
        <div class="section-title-container">
          <div class="section-tag">
            <i class="fas fa-handshake"></i> Rejoignez-nous
          </div>
          <h2>Prêt à booster votre commerce ?</h2>
          <p class="subtitle">Remplissez le formulaire ci-dessous et rejoignez notre réseau de partenaires.</p>
        </div>

        <div class="partner-contact-form active">
          <form id="partner-form">
            <div class="form-header">
              <h3>Demande de partenariat</h3>
              <p>Laissez-nous vos coordonnées et un conseiller vous contactera sous 24h.</p>
            </div>
            <div class="form-content">
              <div class="form-fields">
                <div class="form-field">
                  <input type="text" placeholder="Nom de l'établissement" name="establishmentName" required>
                </div>
                <div class="form-field">
                  <input type="email" placeholder="Email professionnel" name="email" required>
                </div>
                <div class="form-field">
                  <input type="tel" placeholder="Téléphone" name="phone" required>
                </div>
                <div class="form-field">
                  <select name="establishmentType" required>
                    <option value="" disabled selected>Type d'établissement</option>
                    <option value="Café / Restaurant">Café / Restaurant</option>
                    <option value="Hôtel">Hôtel</option>
                    <option value="Commerce">Commerce</option>
                    <option value="Autre">Autre</option>
                  </select>
                  </div>
              </div>
              <button type="submit" class="btn btn-primary partner-submit" id="partner-submit-button">
                <i class="fas fa-paper-plane"></i>
                Envoyer ma demande
              </button>
              <div id="partner-form-message" style="margin-top: 1rem; text-align: center; min-height: 1.5em; color: inherit;"></div>
            </div>
            <div class="form-alternative">
              <span>Vous préférez nous parler directement ? Appelez-nous au </span>
              <a href="tel:+33123456789" class="phone-link">01 23 45 67 89</a>
            </div>
          </form>
        </div>
      </div>
    </div>
  </section>

  <footer id="footer">
  <div class="footer-bg"></div>
  <div class="container">
  <div class="footer-content">
  <div class="footer-logo-area">
  <a href="index.html" class="footer-logo">
  <img src="images/logo-powair.png" alt="Pow'air Logo Icon"> Pow<span>'air</span>
  </a>
  <p class="footer-description">
  Rejoignez le réseau de recharge mobile #1 en France et faites partie de la révolution énergétique.
  </p>
  <div class="footer-social">
  <a href="#" class="social-icon" aria-label="Facebook" target="_blank" rel="noopener noreferrer"><i class="fab fa-facebook-f"></i></a>
  <a href="#" class="social-icon" aria-label="Instagram" target="_blank" rel="noopener noreferrer"><i class="fab fa-instagram"></i></a>
  <a href="#" class="social-icon" aria-label="Twitter" target="_blank" rel="noopener noreferrer"><i class="fab fa-twitter"></i></a>
  <a href="#" class="social-icon" aria-label="LinkedIn" target="_blank" rel="noopener noreferrer"><i class="fab fa-linkedin-in"></i></a>
  </div>
  </div>
  <div>
  <h4 class="footer-title">Navigation</h4>
  <ul class="footer-links">
  <li><a href="#pourquoi-partenaire">Pourquoi devenir partenaire</a></li>
  <li><a href="#avantages">Vos avantages</a></li>
  <li><a href="#fonctionnement">Fonctionnement</a></li>
  <li><a href="#faq">FAQ</a></li>
  <li><a href="#contact">Contact</a></li>
  </ul>
  </div>
  <div>
  <h4 class="footer-title">Ressources</h4>
  <ul class="footer-links">
  <li><a href="#">Kit marketing partenaires</a></li>
  <li><a href="#">Guide d'installation</a></li>
  <li><a href="#">Études de cas</a></li>
  <li><a href="#">Support technique</a></li>
  <li><a href="#">Blog partenaires</a></li>
  </ul>
  </div>
  <div>
  <h4 class="footer-title">Contactez-nous</h4>
  <div class="footer-contact">
  <div class="contact-item">
  <div class="contact-icon"><i class="fas fa-map-marker-alt"></i></div>
  <span class="contact-text">123 Rue Fictive,<br>75000 Paris, France</span>
  </div>
  <div class="contact-item">
  <div class="contact-icon"><i class="fas fa-envelope"></i></div>
  <span class="contact-text"><a href="mailto:partenaires@powair.example.com">partenaires@powair.example.com</a></span>
  </div>
  <div class="contact-item">
  <div class="contact-icon"><i class="fas fa-phone"></i></div>
  <span class="contact-text"><a href="tel:+33123456789">+33 1 23 45 67 89</a></span>
  </div>
  </div>
  </div>
  </div>
  <div class="footer-divider"></div>
  <div class="footer-bottom">
  <p class="footer-copyright">
  © 2024 Pow'air. Tous droits réservés. Développé avec <i class="fas fa-heart"></i>.
  </p>
  <div class="footer-links-bottom">
  <a href="#">Conditions</a>
  <a href="#">Confidentialité</a>
  <a href="#">Plan du site</a>
  </div>
  </div>
  </div>
 </footer>

  <script src="script.js"></script>

  <script>
    document.addEventListener('DOMContentLoaded', () => {
      // Animation des compteurs de statistiques (si présents - non inclus dans ce snippet)
      // ... (Le code d'animation des compteurs .metric-number irait ici s'il y avait une section metrics)

      // Gestion des FAQ
      const faqQuestions = document.querySelectorAll('.faq-question');
      faqQuestions.forEach(question => {
        question.addEventListener('click', () => {
          const answer = question.nextElementSibling;
          const isActive = question.classList.contains('active');
          const icon = question.querySelector('i'); // Cible l'icône

          // Ferme toutes les réponses SAUF celle cliquée si elle était déjà ouverte
          document.querySelectorAll('.faq-question').forEach(q => {
              if (q !== question) {
                  q.classList.remove('active');
                  q.querySelector('i').style.transform = 'rotate(0deg)'; // Réinitialise l'icône des autres
                  q.setAttribute('aria-expanded', 'false'); // Met à jour ARIA pour les autres
                  q.removeAttribute('aria-controls');
              }
          });
          document.querySelectorAll('.faq-answer').forEach(a => {
             if (a !== answer || isActive) { // Cache les autres réponses ou celle cliquée si on la ferme
               a.classList.remove('active');
             }
          });

          // Ouvre/ferme la réponse cliquée
          if (!isActive) {
            question.classList.add('active');
            answer.classList.add('active');
            if (icon) icon.style.transform = 'rotate(180deg)';
             // Gestion ARIA
            question.setAttribute('aria-expanded', 'true');
            if(answer.id) question.setAttribute('aria-controls', answer.id); // Lie la question à la réponse si ID existe
          } else {
             question.classList.remove('active');
             answer.classList.remove('active');
             if (icon) icon.style.transform = 'rotate(0deg)';
              // Gestion ARIA
             question.setAttribute('aria-expanded', 'false');
             question.removeAttribute('aria-controls');
          }
        });

        // Ajout ARIA initial
        question.setAttribute('aria-expanded', 'false');
        const answer = question.nextElementSibling;
        if(answer && !answer.id) { // Donne un ID unique si la réponse n'en a pas
           answer.id = `faq-answer-${Math.random().toString(36).substring(2, 9)}`;
        }
         // Assurez-vous que la réponse a l'ID référencé si elle existe
         if (answer && answer.id && !question.getAttribute('aria-controls')){
           // Ne rien faire si déjà lié ou si la réponse n'a pas d'ID
         } else if (answer && !answer.id) {
            // Géré ci-dessus
         }


      });

      // Gestion du formulaire partenaire (repris du script principal, qui devrait idéalement contenir TOUT le JS)
      const partnerForm = document.getElementById('partner-form');
      const partnerSubmitButton = document.getElementById('partner-submit-button');
      const partnerFormMessage = document.getElementById('partner-form-message');

      if (partnerForm && partnerSubmitButton && partnerFormMessage) {
        partnerForm.addEventListener('submit', async (event) => {
          event.preventDefault();

          partnerSubmitButton.disabled = true;
          partnerSubmitButton.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Envoi en cours...';
          partnerFormMessage.textContent = '';
          partnerFormMessage.style.color = 'inherit';
           partnerFormMessage.removeAttribute('aria-live'); // Supprime pour éviter l'annonce du vide

          const formData = new FormData(partnerForm);
          const data = Object.fromEntries(formData.entries());

          try {
             // REMPLACER par l'appel API réel vers '/api/submit-partner-form' comme dans script.js
             // Simulation d'envoi pour l'exemple
             await new Promise(resolve => setTimeout(resolve, 1500));
             const response = { ok: true }; // Simule une réponse OK
             const result = { message: "Merci ! Votre demande a bien été envoyée. Un conseiller vous contactera dans les 24h."}; // Simule un message de succès

             // Décommentez ces lignes pour l'appel réel
             /*
             const response = await fetch('/api/submit-partner-form', {
               method: 'POST',
               headers: { 'Content-Type': 'application/json' },
               body: JSON.stringify(data)
             });
             const result = await response.json();
             */

            if (response.ok) {
              partnerFormMessage.textContent = result.message || "Merci ! Votre demande a bien été envoyée.";
              partnerFormMessage.style.color = 'var(--accent-color)';
               partnerFormMessage.setAttribute('aria-live', 'polite'); // Annonce le succès
              partnerForm.reset();
            } else {
              partnerFormMessage.textContent = result.error || `Erreur ${response.status}: Veuillez réessayer.`;
              partnerFormMessage.style.color = '#E53E3E';
               partnerFormMessage.setAttribute('aria-live', 'assertive'); // Annonce l'erreur (plus urgent)
            }

          } catch (error) {
            console.error("Erreur lors de l'envoi:", error);
             if (error instanceof TypeError && error.message.includes('Failed to fetch')) {
               partnerFormMessage.textContent = "Erreur: Impossible de contacter le serveur. Vérifiez l'URL ou l'état du serveur.";
             } else {
               partnerFormMessage.textContent = "Une erreur réseau est survenue. Vérifiez votre connexion et réessayez.";
             }
            partnerFormMessage.style.color = '#E53E3E';
            partnerFormMessage.setAttribute('aria-live', 'assertive'); // Annonce l'erreur
          } finally {
            partnerSubmitButton.disabled = false;
            partnerSubmitButton.innerHTML = '<i class="fas fa-paper-plane"></i> Envoyer ma demande';
          }
        });
      }
    });
  </script>
</body>

</html>

--- Fin du fichier : lading_partner.html ---

--- Début du fichier : style.css ---

/* style.css */
:root {
  /* === PALETTE GRADIENT BLEU/CYAN === */
  --gradient-start-color: #4C86F9; /* Bleu vif */
  --gradient-end-color: #00C4FF;   /* Cyan vif */
  --gradient-primary: linear-gradient(135deg, var(--gradient-start-color), var(--gradient-end-color));
  --gradient-primary-hover: linear-gradient(135deg, #3A7BD5, #00B3E6); /* Un peu plus sombre/saturé */

  --gradient-start-color-rgb: 76, 134, 249; /* RGB du bleu de départ */
  --main-white: #FFFFFF;
  --main-white-rgb: 255, 255, 255;
  --dark-bg-base: #121212; /* Fond très sombre pour dark mode */
  --dark-bg-alt: #1E1E1E;  /* Fond alternatif un peu moins sombre */

  /* Nuances mode clair */
  --text-color-light: #111827; /* Noir doux */
  --text-light-light: #5a7a9e; /* Gris-bleu doux pour texte secondaire */
  --bg-color-light: var(--main-white);
  --bg-alt-light: #F8FAFC; /* Gris très clair */
  --border-color-light: #E5E7EB; /* Gris clair pour bordures */

  /* Nuances mode "sombre" (base gris très sombre) */
  --text-color-dark: #F9FAFB; /* Presque blanc */
  --text-light-dark: #A8D8FF; /* Cyan/Bleu très clair pour texte secondaire */
  --bg-color-dark: var(--dark-bg-base);
  --bg-alt-dark: var(--dark-bg-alt);
  --border-color-dark: #374151; /* Gris foncé pour bordures */

  /* Ombres ajustées (basées sur le bleu de départ du gradient) */
  --shadow-sm: 0 2px 5px rgba(var(--gradient-start-color-rgb), 0.08);
  --shadow: 0 5px 15px rgba(var(--gradient-start-color-rgb), 0.1);
  --shadow-md: 0 8px 25px rgba(var(--gradient-start-color-rgb), 0.12);
  --shadow-lg: 0 15px 35px rgba(var(--gradient-start-color-rgb), 0.15);
  --shadow-xl: 0 20px 50px rgba(var(--gradient-start-color-rgb), 0.18);

  /* Variables par défaut (mode clair) */
  --text-color: var(--text-color-light);
  --text-light: var(--text-light-light);
  --bg-color: var(--bg-color-light);
  --bg-alt: var(--bg-alt-light);
  --border-color: var(--border-color-light);
  --accent-color: var(--gradient-start-color); /* Couleur solide pour textes/icônes */

  /* Rayons de bordure (inchangés) */
  --radius-xs: 0.25rem;
  --radius-sm: 0.375rem;
  --radius: 0.5rem;
  --radius-md: 0.75rem;
  --radius-lg: 1rem;
  --radius-xl: 1.5rem;
  --radius-2xl: 2rem;
  --radius-full: 9999px;
}

/* Mode "Sombre" */
.dark-mode {
  --text-color: var(--text-color-dark);
  --text-light: var(--text-light-dark);
  --bg-color: var(--bg-color-dark);
  --bg-alt: var(--bg-alt-dark);
  --border-color: var(--border-color-dark);
  --accent-color: var(--gradient-end-color); /* Utiliser le Cyan en mode sombre pour plus de peps */

  /* Ombres ajustées pour le mode sombre (plus subtiles, basées sur blanc/cyan très léger) */
  --shadow-sm: 0 2px 5px rgba(var(--main-white-rgb), 0.04);
  --shadow: 0 5px 15px rgba(var(--main-white-rgb), 0.06);
  --shadow-md: 0 8px 25px rgba(var(--main-white-rgb), 0.07);
  --shadow-lg: 0 15px 35px rgba(var(--main-white-rgb), 0.08);
  --shadow-xl: 0 20px 50px rgba(var(--main-white-rgb), 0.1);
}

* { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Plus Jakarta Sans', sans-serif; }
html { scroll-behavior: smooth; scroll-padding-top: 80px; }
body { color: var(--text-color); background-color: var(--bg-color); line-height: 1.6; overflow-x: hidden; font-size: 16px; transition: background-color 0.5s ease, color 0.5s ease; }
.container { width: 100%; max-width: 1440px; margin: 0 auto; padding: 0 32px; position: relative; }
section { padding: 100px 0; position: relative; overflow: hidden; }
.section-inner { position: relative; z-index: 2; }

/* Typographie */
h1, h2, h3, h4 { line-height: 1.1; font-weight: 800; letter-spacing: -0.02em; color: var(--text-color); }
h1 { font-size: clamp(3rem, 5vw, 4.5rem); margin-bottom: 1.5rem; }
h2 { font-size: clamp(2.25rem, 4vw, 3rem); margin-bottom: 1.5rem; text-align: center; }
h3 { font-size: clamp(1.5rem, 3vw, 1.8rem); margin-bottom: 1rem; }
p { margin-bottom: 1.5rem; color: var(--text-light); font-size: 1.1rem; line-height: 1.7; }
.subtitle { font-size: clamp(1.1rem, 2vw, 1.25rem); color: var(--text-light); text-align: center; max-width: 800px; margin: -1rem auto 3rem; font-weight: 500; }

/* Mode Switch */
.mode-switch { position: fixed; bottom: 30px; right: 30px; width: 60px; height: 60px; border-radius: 50%; background-image: var(--gradient-primary); background-color: var(--gradient-start-color); /* Fallback */ display: flex; align-items: center; justify-content: center; cursor: pointer; z-index: 999; box-shadow: var(--shadow-md); transition: all 0.3s ease; }
.mode-switch i { color: var(--main-white); font-size: 1.5rem; transition: all 0.3s ease; }
.mode-switch:hover { transform: scale(1.1); box-shadow: var(--shadow-lg); }

/* Header */
header { background-color: rgba(var(--main-white-rgb), 0.9); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); position: fixed; width: 100%; top: 0; left: 0; z-index: 1000; transition: all 0.5s ease; border-bottom: 1px solid var(--border-color); }
.dark-mode header { background-color: rgba(18, 18, 18, 0.9); border-bottom: 1px solid var(--border-color); } /* Dark bg for header */
header.scrolled { background-color: rgba(var(--main-white-rgb), 0.95); box-shadow: var(--shadow-sm); }
.dark-mode header.scrolled { background-color: rgba(18, 18, 18, 0.95); }
.header-content { display: flex; justify-content: space-between; align-items: center; padding: 1rem 0; transition: padding 0.3s ease; flex-wrap: nowrap; }
header.scrolled .header-content { padding: 0.75rem 0; }
.logo { display: flex; align-items: center; font-weight: 800; font-size: 1.5rem; color: var(--text-color); z-index: 1001; text-decoration: none; flex-shrink: 0; margin-right: auto; text-shadow: 0 1px 3px rgba(var(--gradient-start-color-rgb), 0.1); text-transform: uppercase; }
/*.logo span { color: var(--accent-color); } */
/*.logo-icon { margin-right: 0.5rem; font-size: 1.75rem; color: var(--accent-color); }*/
nav { display: flex; align-items: center; gap: 1rem; }
nav ul { display: flex; list-style: none; gap: 1.5rem; }
nav ul li a { text-decoration: none; color: var(--text-color); font-weight: 700; font-size: 1rem; transition: color 0.2s ease; position: relative; padding-bottom: 4px; }
nav ul li a::after { content: ''; position: absolute; width: 0; height: 2px; bottom: 0; left: 0; background: var(--accent-color); transition: width 0.2s ease; }
nav ul li a:hover { color: var(--accent-color); }
nav ul li a:hover::after { width: 100%; }

/* Boutons */
.btn { display: inline-flex; align-items: center; justify-content: center; padding: 0.875rem 1.75rem; border-radius: var(--radius-full); text-decoration: none; font-weight: 600; font-size: 1rem; cursor: pointer; transition: all 0.3s ease; text-align: center; position: relative; overflow: hidden; gap: 0.5rem; white-space: nowrap; border: none; z-index: 1; }
.btn::before { content: ''; position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: rgba(var(--main-white-rgb), 0.1); transform: translateY(-100%); transition: transform 0.3s ease; z-index: -1; }
.dark-mode .btn::before { background: rgba(0, 0, 0, 0.1); }
.btn:hover::before { transform: translateY(0); }
.btn.btn-primary { background-image: var(--gradient-primary); background-color: var(--gradient-start-color); /* Fallback */ color: var(--main-white); box-shadow: 0 4px 15px rgba(var(--gradient-start-color-rgb), 0.2); flex-shrink: 0; }
.btn-primary:hover { transform: translateY(-3px); box-shadow: var(--shadow-md); background-image: var(--gradient-primary-hover); background-color: #3A7BD5; /* Fallback hover */ }
.btn-secondary { background-color: var(--bg-alt); color: var(--accent-color); box-shadow: var(--shadow-sm); border: 1px solid var(--border-color); }
.dark-mode .btn-secondary { background-color: var(--bg-alt); border: 1px solid var(--border-color); color: var(--accent-color); }
.btn-secondary:hover { background-color: var(--border-color); transform: translateY(-3px); box-shadow: var(--shadow); }
.dark-mode .btn-secondary:hover { background-color: var(--border-color); color: var(--text-color); }

/* Menu Mobile */
.mobile-menu-button { background: none; border: none; color: var(--text-light); font-size: 1.8rem; cursor: pointer; z-index: 1001; padding: 5px; display: none; }
.mobile-menu { position: fixed; top: 0; right: 0; width: 100%; height: 100vh; background-color: var(--bg-color); z-index: 1000; transform: translateX(100%); transition: transform 0.5s cubic-bezier(0.77, 0, 0.175, 1); display: flex; flex-direction: column; justify-content: center; align-items: center; padding-top: 60px; overflow-y: auto; }
.mobile-menu.active { transform: translateX(0); }
.mobile-menu ul { list-style: none; text-align: center; padding: 0; width: 100%; }
.mobile-menu ul li { margin-bottom: 1.5rem; opacity: 0; transform: translateY(20px); transition: opacity 0.5s ease, transform 0.5s ease; }
.mobile-menu.active ul li { opacity: 1; transform: translateY(0); transition-delay: calc(0.1s * var(--i)); }
.mobile-menu ul li a { text-decoration: none; color: var(--text-color); font-weight: 700; font-size: 1.8rem; position: relative; display: inline-block; padding-bottom: 5px; padding-left: 1rem; padding-right: 1rem; }
.mobile-menu ul li .btn { font-size: 1.5rem; padding: 0.8rem 1.5rem; margin-top: 1rem; width: auto; max-width: 80%; }
.mobile-menu ul li a::after { content: ''; position: absolute; width: 0; height: 2px; bottom: 0; left: 50%; transform: translateX(-50%); background: var(--accent-color); transition: width 0.3s ease; }
.mobile-menu ul li a:hover::after { width: 80%; }

/* Effet Fond Dégradé Flottant (Adapté) */
.gradient-bg { position: fixed; top: 0; left: 0; width: 100%; height: 100%; overflow: hidden; z-index: -2; }
.gradient-orb { position: absolute; border-radius: 50%; filter: blur(180px); opacity: 0.3; transition: all 5s ease; }
.dark-mode .gradient-orb { opacity: 0.2; filter: blur(200px); }
.gradient-orb:nth-child(1) { top: -30%; left: -20%; width: 70vw; height: 70vw; background-color: var(--gradient-start-color); animation: floatBG 25s ease-in-out infinite alternate; }
.gradient-orb:nth-child(2) { bottom: -40%; right: -30%; width: 80vw; height: 80vw; background-color: var(--gradient-end-color); animation: floatBG 30s ease-in-out infinite alternate 1s; }
.gradient-orb:nth-child(3) { top: 40%; left: 50%; transform: translateX(-50%); width: 60vw; height: 60vw; background-color: var(--main-white); animation: floatBG 28s ease-in-out infinite alternate 2s; opacity: 0.15; }
.dark-mode .gradient-orb:nth-child(3) { background-color: var(--gradient-start-color); opacity: 0.08; }
@keyframes floatBG { from { transform: translate(0, 0) scale(1); } to { transform: translate(30px, -40px) scale(1.15); } }

/* Hero Section */
.hero { min-height: 75vh; display: flex; align-items: center; padding-top: 120px; padding-bottom: 100px; position: relative; overflow: visible; }
.hero-content { display: grid; grid-template-columns: 1fr minmax(auto, 600px); align-items: center; gap: 4rem; }
.hero-text { grid-column: 1 / 2; grid-row: 1 / 2; max-width: 600px; position: relative; z-index: 2; opacity: 0; transform: translateY(50px); transition: opacity 1s ease, transform 1s ease; }
.hero-text.active { opacity: 1; transform: translateY(0); }
.hero-text h1 span { color: var(--accent-color); }
.hero-text p { font-size: 1.2rem; margin-bottom: 2.5rem; max-width: 540px; color: var(--text-light); }
.hero-badge { display: inline-flex; align-items: center; padding: 0.5rem 1rem; background-color: var(--bg-alt); border-radius: var(--radius-full); box-shadow: var(--shadow); margin-bottom: 1.5rem; font-weight: 600; font-size: 0.875rem; color: var(--accent-color); border: 1px solid var(--border-color); transition: all 0.3s ease; }
.hero-badge:hover { transform: translateY(-5px); box-shadow: var(--shadow-md); }
.hero-badge i { margin-right: 0.5rem; color: var(--accent-color); }
.hero-image { grid-column: 2 / 3; grid-row: 1 / 3; position: relative; height: auto; max-width: 600px; width: 100%; opacity: 0; transform: translateY(50px); transition: opacity 1s ease 0.3s, transform 1s ease 0.3s; aspect-ratio: 1 / 1; display: flex; align-items: center; justify-content: center; }
.hero-image.active { opacity: 1; transform: translateY(0); }
.hero-phone-container { position: relative; width: 90%; max-width: 450px; aspect-ratio: 3 / 4; perspective: 1000px; }
.hero-phone { position: absolute; width: 65%; height: 95%; top: 50%; left: 50%; transform: translate(-50%, -50%) rotateY(-15deg) rotateX(10deg); background-color: var(--bg-color); border-radius: 2rem; overflow: hidden; box-shadow: var(--shadow-lg); transition: transform 0.5s ease; will-change: transform; border: 6px solid var(--text-color); }
.dark-mode .hero-phone { border-color: var(--border-color); } /* Bordure plus subtile en dark */
.hero-phone-notch { position: absolute; top: 0.5rem; left: 50%; transform: translateX(-50%); width: 35%; height: 1rem; background-color: var(--text-color); border-radius: 0 0 var(--radius-sm) var(--radius-sm); z-index: 1; }
.dark-mode .hero-phone-notch { background-color: var(--border-color); }
.hero-phone-screen {
  width: 100%;      /* Conserve la largeur à 100% du conteneur */
  /* height: 100%; */ /* Supprimez ou commentez cette ligne */
  height: auto;       /* Ajoutez cette ligne */
  /* object-fit: cover; */ /* Supprimez ou commentez cette ligne */
  display: block;     /* Assurez-vous que l'image se comporte comme un bloc */
  aspect-ratio: 10 / 19;
}
.hero-buttons { grid-column: 1 / 2; grid-row: 2 / 3; display: flex; gap: 1rem; flex-wrap: wrap; margin-top: 0; opacity: 0; transform: translateY(50px); transition: opacity 1s ease 0.2s, transform 1s ease 0.2s; }
.hero-buttons.active { opacity: 1; transform: translateY(0); }
/* Scroll Down */
.scroll-down { position: absolute; bottom: -5rem; left: 50%; transform: translateX(-50%) translateY(30px); display: flex; flex-direction: column; align-items: center; opacity: 0; transition: opacity 1s ease 1.5s, transform 1s ease 1.5s; z-index: 5; cursor: default; }
.scroll-down.active { opacity: 1; transform: translateX(-50%) translateY(0); }
.scroll-down-text { font-size: 0.875rem; font-weight: 500; color: var(--text-light); margin-bottom: 0.5rem; }
.scroll-down-arrow { width: 2.5rem; height: 2.5rem; background-color: var(--bg-color); border-radius: 50%; display: flex; align-items: center; justify-content: center; box-shadow: var(--shadow); animation: bounce 2s infinite; border: 1px solid var(--border-color); }
.scroll-down-arrow i { color: var(--accent-color); font-size: 1rem; }

/* Section BG Pattern */
.section-bg { position: absolute; top: 0; left: 0; width: 100%; height: 100%; opacity: 0.06; z-index: -1; pointer-events: none; }
.dark-mode .section-bg { opacity: 0.04; }
.section-bg-pattern { width: 100%; height: 100%; background-image: radial-gradient(var(--border-color) 1px, transparent 1px); background-size: 30px 30px; }
.dark-mode .section-bg-pattern { background-image: radial-gradient(var(--border-color) 1px, transparent 1px); }

/* Section Title */
.section-title-container { position: relative; margin-bottom: 4rem; text-align: center; opacity: 0; transform: translateY(30px); transition: opacity 0.6s ease, transform 0.6s ease; }
.section-title-container.active { opacity: 1; transform: translateY(0); }
.section-tag { display: inline-flex; align-items: center; padding: 0.5rem 1rem; background-color: var(--bg-alt); border-radius: var(--radius-full); font-weight: 600; font-size: 0.875rem; color: var(--accent-color); margin-bottom: 1rem; border: 1px solid var(--border-color); }
.section-tag i { margin-right: 0.5rem; color: var(--accent-color); }

/* How it works */
.how-it-works { padding-top: 80px; }
.steps { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2.5rem; margin-top: 4rem; }
.step { padding: 2.5rem 2rem; background-color: var(--bg-color); border-radius: var(--radius-xl); box-shadow: var(--shadow); transition: transform 0.4s ease, box-shadow 0.4s ease, border-color 0.4s ease, opacity 0.5s ease; position: relative; z-index: 1; text-align: center; border: 1px solid var(--border-color); overflow: hidden; opacity: 0; transform: translateY(50px); }
.step.active { opacity: 1; transform: translateY(0); }
.step:nth-child(2).active { transition-delay: 0.15s; }
.step:nth-child(3).active { transition-delay: 0.3s; }
.step:hover { transform: translateY(-10px); box-shadow: var(--shadow-lg); border-color: var(--accent-color); }
.step-number { position: absolute; top: 1.5rem; right: 1.5rem; width: 2.5rem; height: 2.5rem; background-image: var(--gradient-primary); background-color: var(--gradient-start-color); color: var(--main-white); border-radius: var(--radius-full); display: flex; align-items: center; justify-content: center; font-weight: 800; font-size: 1.1rem; box-shadow: var(--shadow-sm); border: 2px solid var(--bg-color); z-index: 2; }
.step-title { font-size: 1.5rem; font-weight: 700; margin-top: 1rem; margin-bottom: 0.8rem; color: var(--text-color); }
.step-description { color: var(--text-light); font-size: 1rem; line-height: 1.6; margin-bottom: 0; }
.step-illustration { height: 180px; margin-bottom: 1.5rem; display: flex; align-items: center; justify-content: center; }
.step-illustration img { max-height: 100%; max-width: 90%; width: auto; height: auto; object-fit: contain; border-radius: var(--radius-md); }

/* Avantages */
.benefits { position: relative; background-color: var(--bg-alt); overflow: visible; }
.benefits .section-inner { background-color: var(--bg-color); border-radius: var(--radius-2xl); padding: 4rem; box-shadow: 0 10px 30px rgba(var(--gradient-start-color-rgb), 0.08), 0 0 60px rgba(var(--gradient-start-color-rgb), 0.15); position: relative; z-index: 2; margin-top: -20px; margin-bottom: 20px; border: 1px solid var(--border-color); }
.dark-mode .benefits .section-inner { box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2), 0 0 60px rgba(var(--gradient-start-color-rgb), 0.1); }
.benefits-bg { position: absolute; width: 100%; height: 100%; top: 0; left: 0; z-index: 0; opacity: 0.15; overflow: hidden; pointer-events: none; }
.dark-mode .benefits-bg { opacity: 0.1; }
.benefits-bg-shape { position: absolute; width: 50vw; height: 50vw; border-radius: 50%; filter: blur(150px); opacity: 0.5; }
.dark-mode .benefits-bg-shape { opacity: 0.8; }
.benefits-bg-shape:nth-child(1) { top: -25%; right: -25%; background: linear-gradient(135deg, var(--gradient-end-color), var(--bg-color)); }
.benefits-bg-shape:nth-child(2) { bottom: -25%; left: -25%; background: linear-gradient(135deg, var(--gradient-start-color), var(--gradient-end-color)); }
.dark-mode .benefits-bg-shape:nth-child(1) { background: linear-gradient(135deg, rgba(0, 196, 255, 0.1), rgba(18, 18, 18, 0.1)); }
.dark-mode .benefits-bg-shape:nth-child(2) { background: linear-gradient(135deg, rgba(76, 134, 249, 0.1), rgba(0, 196, 255, 0.1)); }
.benefits-list { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 2rem; margin-top: 4rem; }
.benefit-card { border-radius: var(--radius-xl); box-shadow: var(--shadow-sm); transition: transform 0.4s ease, box-shadow 0.4s ease, border-color 0.4s ease, opacity 0.5s ease; overflow: hidden; position: relative; border: 1px solid var(--border-color); opacity: 0; transform: translateY(30px); background-color: var(--bg-color); }
.benefit-card.active { opacity: 1; transform: translateY(0); }
.benefit-card:nth-child(2).active { transition-delay: 0.1s; }
.benefit-card:nth-child(3).active { transition-delay: 0.2s; }
/* La 4ème carte a été retirée */
.benefit-card:hover { transform: translateY(-10px) scale(1.02); box-shadow: var(--shadow-md); border-color: var(--accent-color); }
.benefit-content { padding: 2rem; position: relative; z-index: 2; }
.benefit-icon { width: 3.5rem; height: 3.5rem; background: var(--bg-alt); border-radius: var(--radius-full); display: flex; align-items: center; justify-content: center; margin-bottom: 1.5rem; transition: all 0.3s ease; border: 1px solid var(--border-color); }
.benefit-card:hover .benefit-icon { background-image: var(--gradient-primary); background-color: var(--gradient-start-color); transform: rotate(15deg); border-color: transparent; }
.benefit-icon i { font-size: 1.5rem; color: var(--accent-color); transition: all 0.3s ease; }
.benefit-card:hover .benefit-icon i { color: var(--main-white); transform: scale(1.1); }
.benefit-title { font-size: 1.3rem; font-weight: 700; margin-bottom: 0.8rem; color: var(--text-color); }
.benefit-description { color: var(--text-light); font-size: 1rem; margin-bottom: 0; }

/* Réseau */
/* --- Bloc CSS pour la section Réseau/Carte --- */

.network {
  position: relative;
  overflow: hidden; /* Gardé pour le moment, pourrait être testé en le supprimant si le z-index ne résout rien */
}

.map-container {
  position: relative;
  width: 100%;
  margin-top: 4rem;
  opacity: 0; /* Géré par l'animation au scroll via .active */
  transform: translateY(30px); /* Géré par l'animation au scroll */
  transition: opacity 0.6s ease, transform 0.6s ease;
  /* z-index: 2; */ /* Le z-index ici pourrait entrer en conflit, mettons le sur le wrapper */
}

.map-container.active {
  opacity: 1;
  transform: translateY(0);
}

.map-wrapper {
  width: 100%;
  height: 500px; /* Hauteur du conteneur de carte */
  background-color: var(--border-color); /* Couleur de fond pendant le chargement */
  border-radius: var(--radius-xl);
  overflow: hidden; /* Gardé pour le moment */
  box-shadow: var(--shadow-lg);
  position: relative; /* IMPORTANT: Nécessaire pour que z-index fonctionne */
  z-index: 100; /* TEST : Valeur élevée pour essayer de passer au-dessus d'autres éléments */
}

.dark-mode .map-wrapper {
  background-color: var(--dark-bg-alt);
}

#map {
  /* width: 1000px; */ /* Votre valeur précédente - 100% est généralement plus sûr */
  width: 100%;    /* Prend toute la largeur du .map-wrapper */
  height: 500px;   /* Hauteur explicite, comme vous l'aviez mise */
  position: relative; /* Assure son propre contexte d'empilement */
  z-index: auto;    /* Laisse Leaflet gérer les z-index internes, .map-wrapper gère la superposition globale */
  background-color: #DDDDDD; /* Léger fond gris pour voir si le div #map lui-même est visible */
}

#map p {
  /* Style pour le texte placeholder - Normalement plus visible car le div #map est vidé par JS */
  color: var(--text-light);
  margin-top: 50px;
  text-align: center;
}

/* Styles pour la recherche sur la carte avec autocomplétion */
.map-search-container {
  position: absolute;
  bottom: 20px;
  left: 50%;
  transform: translateX(-50%);
  width: 100%;
  max-width: 400px;
  z-index: 1000;
  padding: 0 15px;
}

.map-search-wrapper {
  display: flex;
  background-color: var(--bg-color);
  border-radius: var(--radius-full);
  box-shadow: var(--shadow-lg);
  border: 1px solid var(--border-color);
  overflow: visible; /* Changé de 'hidden' à 'visible' pour permettre aux suggestions de déborder */
  transition: all 0.3s ease;
  position: relative; /* Pour positionner les suggestions */
}

.map-search-wrapper:focus-within {
  box-shadow: var(--shadow-xl);
  border-color: var(--accent-color);
}

.map-search-input {
  flex-grow: 1;
  padding: 0.875rem 1.5rem;
  border: none;
  outline: none;
  font-size: 16px; /* Taille minimale pour empêcher le zoom sur iOS */
  background-color: transparent;
  color: var(--text-color);
  border-radius: var(--radius-full) 0 0 var(--radius-full);
  touch-action: manipulation; /* Empêche certains comportements tactiles indésirables */
  -webkit-appearance: none; /* Désactive l'apparence par défaut sur iOS */
}

.map-search-input::placeholder {
  color: var(--text-light);
}

.map-search-button {
  border-radius: 0 var(--radius-full) var(--radius-full) 0;
  padding: 0.875rem 1.25rem;
  margin: 0;
}

/* Styles pour les suggestions */
.search-suggestions {
  position: absolute;
  top: auto;
  bottom: calc(100% + 5px);
  left: 0;
  width: 100%;
  background-color: var(--bg-color);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-lg);
  border: 1px solid var(--border-color);
  max-height: 0;
  overflow-y: auto;
  transition: max-height 0.3s ease, opacity 0.3s ease;
  opacity: 0;
  pointer-events: none;
  z-index: 1001;
}

.search-suggestions.active {
  max-height: 300px;
  opacity: 1;
  pointer-events: auto;
}

.suggestion-item {
  padding: 0.75rem 1.5rem;
  cursor: pointer;
  transition: background-color 0.2s ease;
  display: flex;
  align-items: center;
  gap: 0.75rem;
  border-bottom: 1px solid var(--border-color);
}

.suggestion-item:last-child {
  border-bottom: none;
}

.suggestion-item:hover,
.suggestion-item.highlighted {
  background-color: var(--bg-alt);
}

.suggestion-item i {
  color: var(--accent-color);
  font-size: 1rem;
  width: 20px;
  text-align: center;
}

.suggestion-item.location-option {
  font-weight: 600;
  color: var(--accent-color);
  border-bottom: 1px solid var(--border-color);
}

.suggestion-item .suggestion-main {
  font-weight: 600;
  color: var(--text-color);
}

.suggestion-item .suggestion-secondary {
  font-size: 0.85rem;
  color: var(--text-light);
  margin-top: 0.1rem;
}

/* Loader pour le chargement des suggestions */
.suggestion-loader {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 1rem;
  color: var(--text-light);
}

/* Responsive */
@media (max-width: 768px) {
  .map-search-container {
    max-width: calc(100% - 30px);
    bottom: 15px;
  }
  
  .map-search-input {
    padding: 0.75rem 1rem;
    font-size: 0.9rem;
  }
  
  .map-search-button {
    padding: 0.75rem 1rem;
  }
  
  .suggestion-item {
    padding: 0.6rem 1rem;
  }
}
/* --- Fin Bloc CSS --- */

/* Tarifs */
.pricing { position: relative; background-color: var(--bg-alt); overflow: hidden; }
.pricing::before, .pricing::after { content: ''; position: absolute; border-radius: 50%; opacity: 0.2; z-index: 0; pointer-events: none; filter: blur(120px); }
.pricing::before { width: 800px; height: 800px; background-color: var(--gradient-start-color); top: -400px; left: -300px; opacity: 0.1; }
.dark-mode .pricing::before { opacity: 0.05; }
.pricing::after { width: 700px; height: 700px; background-color: var(--gradient-end-color); bottom: -350px; right: -250px; opacity: 0.15; }
.dark-mode .pricing::after { opacity: 0.08; }
.pricing .section-inner { position: relative; z-index: 1; }
.pricing-options { display: flex; flex-wrap: wrap; justify-content: center; gap: 2rem; margin-top: 4rem; position: relative; z-index: 2; align-items: stretch; }
.pricing-card { background-color: var(--bg-color); border-radius: var(--radius-xl); box-shadow: var(--shadow); transition: transform 0.4s ease, box-shadow 0.4s ease, border-color 0.4s ease, opacity 0.5s ease; overflow: hidden; position: relative; border: 1px solid var(--border-color); width: 100%; max-width: 350px; display: flex; flex-direction: column; opacity: 0; transform: translateY(30px); }
.pricing-card.active { opacity: 1; transform: translateY(0); }
.pricing-card:nth-child(2).active { transition-delay: 0.15s; }
.pricing-card:nth-child(3).active { transition-delay: 0.3s; }
.pricing-card:hover { transform: translateY(-10px) scale(1.02); box-shadow: var(--shadow-lg); border-color: var(--accent-color); }
.pricing-card.popular { border: 2px solid var(--accent-color); transform: scale(1.05) translateY(0); box-shadow: var(--shadow-md); }
.pricing-card.popular.active { transform: scale(1.05) translateY(0); }
.pricing-card.popular:hover { transform: scale(1.05) translateY(-10px); box-shadow: var(--shadow-xl); }
.popular-badge { position: absolute; top: 1.25rem; right: 1.25rem; background-image: var(--gradient-primary); background-color: var(--gradient-start-color); color: var(--main-white); font-size: 0.75rem; font-weight: 700; padding: 0.375rem 0.75rem; border-radius: var(--radius-full); z-index: 2; }
.pricing-header { padding: 2rem; text-align: center; border-bottom: 1px solid var(--border-color); }
.pricing-name { font-size: 1.4rem; font-weight: 700; margin-bottom: 0.8rem; color: var(--text-color); }
.pricing-price { font-size: 2.8rem; font-weight: 800; line-height: 1; margin-bottom: 0.5rem; display: inline-block; color: var(--accent-color); }
.pricing-period { font-size: 0.875rem; color: var(--text-light); font-weight: 500; display: block; }
.pricing-description { font-size: 0.9rem; color: var(--text-light); margin-top: 1rem; padding: 0 1.5rem; text-align: center; }
.pricing-features { padding: 1.5rem 2rem; display: flex; flex-direction: column; gap: 0.8rem; flex-grow: 1; }
.pricing-feature { display: flex; align-items: flex-start; gap: 0.75rem; }
.pricing-feature i { color: var(--accent-color); font-size: 1.1rem; flex-shrink: 0; margin-top: 0.2em; }
.pricing-feature-text { font-size: 0.9375rem; color: var(--text-color); font-weight: 500; line-height: 1.5; }
.pricing-action { padding: 0 2rem 2rem; margin-top: 1.5rem; text-align: center; }

 /* Partenaires */
.partners { position: relative; overflow: hidden; background: linear-gradient(180deg, var(--bg-color) 0%, var(--bg-alt) 100%); }
.partners-bg { position: absolute; width: 100%; height: 100%; top: 0; left: 0; z-index: 0; overflow: hidden; pointer-events: none; }
.partners-bg-shape { position: absolute; border-radius: 50%; filter: blur(150px); opacity: 0.15; transition: all 8s ease; }
.dark-mode .partners-bg-shape { opacity: 0.1; }
.partners-bg-shape:nth-child(1) { width: 60vw; height: 60vw; top: -20%; right: -30%; background: var(--gradient-start-color); animation: floatPartnerBG 25s ease-in-out infinite alternate; }
.partners-bg-shape:nth-child(2) { width: 50vw; height: 50vw; bottom: -20%; left: -20%; background: var(--gradient-end-color); animation: floatPartnerBG 20s ease-in-out infinite alternate-reverse; opacity: 0.2;}
.dark-mode .partners-bg-shape:nth-child(2) { opacity: 0.15;}
@keyframes floatPartnerBG { 0% { transform: translate(0, 0) rotate(0deg); } 50% { transform: translate(40px, -30px) rotate(8deg); } 100% { transform: translate(-30px, 50px) rotate(-6deg); } }
.partners-benefits, .partners-stats, .partners-testimonials, .partner-process, .partner-contact-form, .partner-brands { max-width: 1100px; margin-left: auto; margin-right: auto; margin-top: 4rem; margin-bottom: 4rem; position: relative; z-index: 1; }
.partners-stats { max-width: 800px; }
.partners-testimonials { max-width: 800px; }
.partner-process { max-width: 700px; }
.partner-contact-form { max-width: 900px; }
.partners-subtitle { font-size: 1.6rem; margin-bottom: 2rem; position: relative; text-align: center; padding-left: 0; display: block; color: var(--text-color); }
.partners-subtitle::before { display: none; }
.partners-cards { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.5rem; margin-top: 2rem; }
.partner-card { background-color: var(--bg-color); border-radius: var(--radius-lg); padding: 1.5rem; box-shadow: var(--shadow); position: relative; overflow: hidden; transition: all 0.4s ease; height: 100%; border: 1px solid var(--border-color); z-index: 1; opacity: 0; transform: translateY(20px); display: flex; flex-direction: column; }
.partner-card-content { flex-grow: 1; }
.partner-card.active { opacity: 1; transform: translateY(0); }
.partner-card:nth-child(2).active { transition-delay: 0.1s; }
.partner-card:nth-child(3).active { transition-delay: 0.2s; }
.partner-card:nth-child(4).active { transition-delay: 0.3s; }
.partner-card:hover { transform: translateY(-10px); box-shadow: var(--shadow-lg); border-color: var(--accent-color); }
.partner-card-bg { position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: linear-gradient(135deg, rgba(var(--gradient-start-color-rgb), 0.1), transparent); opacity: 0.5; z-index: -1; transform: translateX(-100%); transition: transform 0.5s ease; }
.dark-mode .partner-card-bg { background: linear-gradient(135deg, rgba(var(--main-white-rgb), 0.05), transparent); }
.partner-card:hover .partner-card-bg { transform: translateX(0); }
.partner-card-icon { width: 3rem; height: 3rem; background-image: var(--gradient-primary); background-color: var(--gradient-start-color); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin-bottom: 1rem; color: var(--main-white); font-size: 1.2rem; transition: all 0.3s ease; box-shadow: var(--shadow-sm); flex-shrink: 0; border: none; }
.partner-card:hover .partner-card-icon { transform: scale(1.1) rotate(10deg); box-shadow: var(--shadow-md); }
.partner-card h4 { font-size: 1.2rem; margin-bottom: 0.8rem; color: var(--text-color); font-weight: 700; }
.partner-card p { font-size: 0.95rem; color: var(--text-light); margin-bottom: 0; }
.partners-stats { display: flex; justify-content: space-around; background-color: var(--bg-color); border-radius: var(--radius-lg); padding: 2rem; box-shadow: var(--shadow); border: 1px solid var(--border-color); opacity: 0; transform: translateY(20px); transition: opacity 0.6s ease, transform 0.6s ease; }
.partners-stats.active { opacity: 1; transform: translateY(0); }
.stat-item { text-align: center; }
.stat-number { font-size: 2.5rem; font-weight: 800; line-height: 1; margin-bottom: 0.5rem; color: var(--accent-color); }
.stat-label { font-size: 0.9rem; color: var(--text-light); font-weight: 500; }
.partners-testimonials { opacity: 0; transform: translateY(20px); transition: opacity 0.6s ease 0.2s, transform 0.6s ease 0.2s; text-align: center; }
.partners-testimonials.active { opacity: 1; transform: translateY(0); }
.testimonial-slider { position: relative; background-color: var(--bg-color); border-radius: var(--radius-lg); padding: 2rem; box-shadow: var(--shadow); border: 1px solid var(--border-color); overflow: hidden; margin-top: 2rem; text-align: left; max-width: 700px; margin-left: auto; margin-right: auto; }
.testimonial-slide { display: none; animation: fadeIn 0.5s ease; }
.testimonial-slide.active { display: block; }
.testimonial-content p { font-size: 1.1rem; font-style: italic; line-height: 1.6; color: var(--text-color); position: relative; padding: 0 1rem; text-align: center; }
.testimonial-content p::before, .testimonial-content p::after { content: '"'; font-size: 2rem; font-family: 'Georgia', serif; line-height: 0; color: var(--accent-color); opacity: 0.5; position: absolute; }
.testimonial-content p::before { left: 0; top: 0.5rem; }
.testimonial-content p::after { right: 0; bottom: -0.3rem; }
.testimonial-author { display: flex; align-items: center; margin-top: 1.5rem; justify-content: center; }
.testimonial-avatar { width: 3rem; height: 3rem; background-image: var(--gradient-primary); background-color: var(--gradient-start-color); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin-right: 1rem; color: var(--main-white); font-size: 1.2rem; }
.testimonial-info h5 { font-size: 1.1rem; font-weight: 700; margin-bottom: 0.2rem; color: var(--text-color); }
.testimonial-info span { font-size: 0.85rem; color: var(--text-light); }
.testimonial-dots { display: flex; justify-content: center; margin-top: 1.5rem; }
.dot { width: 10px; height: 10px; background-color: var(--border-color); border-radius: 50%; margin: 0 5px; cursor: pointer; transition: all 0.3s ease; }
.dot.active { background-color: var(--accent-color); transform: scale(1.2); }
.partner-process { opacity: 0; transform: translateY(20px); transition: opacity 0.6s ease 0.4s, transform 0.6s ease 0.4s; text-align: center; }
.partner-process.active { opacity: 1; transform: translateY(0); }
.process-steps { background-color: var(--bg-color); border-radius: var(--radius-lg); padding: 2rem; box-shadow: var(--shadow); border: 1px solid var(--border-color); position: relative; margin-top: 2rem; text-align: left; }
.process-steps::before { content: ''; position: absolute; left: 2.5rem; top: 4rem; height: calc(100% - 8rem); width: 2px; background: var(--accent-color); z-index: 1; }
.process-step { display: flex; align-items: flex-start; margin-bottom: 2rem; position: relative; z-index: 2; }
.process-step:last-child { margin-bottom: 0; }
.process-step-number { width: 3rem; height: 3rem; background-image: var(--gradient-primary); background-color: var(--gradient-start-color); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: var(--main-white); font-size: 1.3rem; font-weight: 700; margin-right: 1.5rem; flex-shrink: 0; box-shadow: var(--shadow-sm); border: 3px solid var(--bg-color); transition: all 0.3s ease; }
.process-step:hover .process-step-number { transform: scale(1.1); box-shadow: var(--shadow-md); }
.process-step-content h4 { font-size: 1.2rem; font-weight: 700; margin-bottom: 0.5rem; color: var(--text-color); }
.process-step-content p { font-size: 0.95rem; color: var(--text-light); margin-bottom: 0; }
.partner-contact-form { background: linear-gradient(135deg, var(--bg-alt), #E0F2FE); border-radius: var(--radius-xl); padding: 3rem; position: relative; overflow: hidden; box-shadow: var(--shadow-lg); opacity: 0; transform: translateY(30px); transition: opacity 0.8s ease, transform 0.8s ease; border: 1px solid var(--border-color);}
.dark-mode .partner-contact-form { background: linear-gradient(135deg, var(--bg-alt-dark), var(--bg-color-dark)); border: 1px solid var(--border-color);}
.partner-contact-form.active { opacity: 1; transform: translateY(0); }
.form-header { text-align: center; margin-bottom: 2.5rem; }
.form-header h3 { font-size: 2rem; font-weight: 800; margin-bottom: 1rem; color: var(--text-color); }
.form-header p { font-size: 1.1rem; color: var(--text-light); max-width: 600px; margin: 0 auto; }
.form-content { max-width: 800px; margin: 0 auto; }
.form-fields { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; margin-bottom: 2rem; }
.form-field input, .form-field select { width: 100%; padding: 1rem 1.5rem; border-radius: var(--radius-lg); border: 1px solid var(--border-color); background-color: var(--bg-color); font-size: 1rem; color: var(--text-color); transition: all 0.3s ease; box-shadow: var(--shadow-sm); }
.form-field input::placeholder, .form-field select::placeholder { color: var(--text-light); }
.form-field select:required:invalid { color: var(--text-light); }
.form-field option { color: var(--text-color); background: var(--bg-color); }
.form-field option[disabled] { color: var(--text-light); }
.form-field input:focus, .form-field select:focus { outline: none; border-color: var(--accent-color); box-shadow: 0 0 0 3px rgba(var(--gradient-start-color-rgb), 0.2); }
.partner-submit { display: block; margin: 0 auto; padding: 1rem 2rem; font-size: 1.1rem; }
.form-alternative { text-align: center; margin-top: 2rem; font-size: 1rem; color: var(--text-light); }
.phone-link { color: var(--accent-color); font-weight: 700; text-decoration: none; transition: all 0.3s ease; }
.phone-link:hover { filter: brightness(0.8); text-decoration: underline; }
.partner-brands { text-align: center; opacity: 0; transform: translateY(30px); transition: opacity 0.6s ease, transform 0.6s ease; }
.partner-brands.active { opacity: 1; transform: translateY(0); }
.partner-brands h4 { font-size: 1.3rem; margin-bottom: 2rem; color: var(--text-light); position: relative; display: inline-block; }
.partner-brands h4::after { content: ''; position: absolute; left: 0; bottom: -10px; width: 100%; height: 2px; background: linear-gradient(to right, transparent, var(--accent-color), transparent); }
.partner-logos { display: flex; justify-content: center; flex-wrap: wrap; gap: 3rem; margin-top: 3rem; }
.partner-logo { padding: 1rem 2rem; background-color: var(--bg-color); border-radius: var(--radius-lg); box-shadow: var(--shadow); display: flex; align-items: center; gap: 0.8rem; font-weight: 700; color: var(--text-light); border: 1px solid var(--border-color); transition: all 0.3s ease; }
.partner-logo i { color: var(--accent-color); font-size: 1.5rem; }
.partner-logo:hover { transform: translateY(-10px); box-shadow: var(--shadow-lg); border-color: var(--accent-color); color: var(--text-color); }
.partner-hidden-content { display: none; }
.partner-hidden-content.visible { display: block; animation: fadeIn 0.5s ease-out; }
.partner-toggle-container { text-align: center; margin-top: 2.5rem; margin-bottom: 2.5rem; }
.partner-toggle-button i { margin-right: 0.5rem; transition: transform 0.3s ease; }
.partner-toggle-button[aria-expanded="true"] i { transform: rotate(180deg); }

/* Download */
.download { position: relative; overflow: hidden; padding-bottom: 100px; }
.download-container { position: relative; background: linear-gradient(135deg, var(--bg-alt), #E0F2FE); border-radius: var(--radius-2xl); padding: 4rem; margin-top: 4rem; overflow: hidden; box-shadow: var(--shadow-lg); opacity: 0; transform: translateY(30px); transition: opacity 0.6s ease, transform 0.6s ease; z-index: 2; border: 1px solid var(--border-color);}
.dark-mode .download-container { background: linear-gradient(135deg, var(--bg-alt-dark), var(--bg-color-dark)); border: 1px solid var(--border-color); }
.download-container.active { opacity: 1; transform: translateY(0); }
.download-bg { position: absolute; top: 0; left: 0; width: 100%; height: 100%; z-index: 0; opacity: 0.1; overflow: hidden; pointer-events: none; }
.dark-mode .download-bg { opacity: 0.05; }
.download-bg::before { content: ''; position: absolute; top: -50%; left: -50%; width: 200%; height: 200%; background: radial-gradient(circle, var(--accent-color) 0%, transparent 70%); animation: rotate 40s linear infinite; opacity: 0.5; }
.download-content { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 3rem; align-items: center; position: relative; z-index: 1; }
.download-title { font-size: clamp(2rem, 4vw, 2.5rem); font-weight: 800; margin-bottom: 1.5rem; line-height: 1.2; color: var(--text-color); text-align: left; }
.download-description { font-size: 1.1rem; margin-bottom: 2rem; color: var(--text-light); text-align: left; }
.download-buttons { display: flex; flex-wrap: wrap; gap: 1rem; justify-content: flex-start; }
.app-button { display: flex; align-items: center; gap: 0.75rem; background-color: var(--text-color); color: var(--bg-color); border-radius: var(--radius-lg); padding: 0.75rem 1.5rem; text-decoration: none; transition: transform 0.3s ease, box-shadow 0.3s ease; box-shadow: var(--shadow-sm); }
.app-button:hover { transform: translateY(-3px); box-shadow: var(--shadow-md); }
.dark-mode .app-button { background-color: var(--bg-alt); color: var(--text-color); border: 1px solid var(--border-color);}
.app-button-icon { font-size: 1.8rem; }
.app-button-text { display: flex; flex-direction: column; text-align: left; }
.app-button-label { font-size: 0.75rem; opacity: 0.8; line-height: 1; }
.app-button-store { font-size: 1.1rem; font-weight: 600; line-height: 1.2; }
.download-app-preview { position: relative; text-align: center; perspective: 1000px; }
.app-preview-phone { position: relative; width: 100%; max-width: 280px; margin: 0 auto; transform-style: preserve-3d; transform: rotateY(-15deg) rotateX(5deg); transition: transform 0.5s ease; }
.app-preview-phone:hover { transform: rotateY(0deg) rotateX(0deg) scale(1.05); }
.app-preview-phone-frame {
  width: 100%;
  border-radius: 1.5rem;
  box-shadow: var(--shadow-lg);
  border:none ;
  overflow: hidden;
  background-color: transparent; /* Fond supprimé */
}
.app-preview-screen { width: 100%; height: auto; display: block; aspect-ratio: 9 / 19.5; }

/* Footer */
footer { background-color: var(--dark-bg-base); color: #9CA3AF; /* Gris clair pour texte footer */ padding: 5rem 0 2rem; position: relative; overflow: hidden; border-top: 1px solid var(--border-color-dark); }
.footer-bg { position: absolute; top: 0; left: 0; width: 100%; height: 100%; z-index: 0; pointer-events: none; }
.footer-bg::before { content: ''; position: absolute; top: 0; left: 0; width: 100%; height: 100%; background-image: radial-gradient(var(--border-color-dark) 1px, transparent 1px); background-size: 25px 25px; opacity: 0.05; }
.footer-content { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 3rem; margin-bottom: 4rem; position: relative; z-index: 1; }
.footer-logo-area { grid-column: span 1 / span 2; max-width: 400px; }
.footer-logo { display: flex; align-items: center; font-weight: 800; font-size: 1.5rem; color: var(--text-color-dark); margin-bottom: 1rem; text-decoration: none; text-transform: uppercase; }
/*.footer-logo span { color: var(--gradient-end-color); /* Cyan pour le span */
/*.footer-logo-icon { margin-right: 0.5rem; font-size: 1.75rem; color: var(--gradient-end-color); /* Cyan pour icone */
.footer-description { color: #9CA3AF; font-size: 0.9rem; line-height: 1.6; margin-bottom: 1.5rem; }
.footer-social { display: flex; gap: 1rem; }
.social-icon { width: 2.25rem; height: 2.25rem; background-color: var(--dark-bg-alt); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: #9CA3AF; font-size: 0.9rem; text-decoration: none; transition: all 0.3s ease; border: 1px solid var(--border-color-dark);}
.social-icon:hover { background-image: var(--gradient-primary); background-color: var(--gradient-start-color); color: var(--main-white); transform: translateY(-3px); border-color: transparent;}
.footer-title { font-size: 1.1rem; font-weight: 700; color: var(--text-color-dark); margin-bottom: 1.25rem; position: relative; padding-left: 0.75rem; }
.footer-title::before { content: ''; position: absolute; top: 50%; transform: translateY(-50%); left: 0; width: 3px; height: 70%; background: var(--gradient-end-color); border-radius: var(--radius-full); }
.footer-links { list-style: none; padding: 0; }
.footer-links li { margin-bottom: 0.6rem; }
.footer-links a { color: #9CA3AF; text-decoration: none; font-size: 0.9rem; transition: color 0.3s ease, transform 0.3s ease; display: inline-block; }
.footer-links a:hover { color: var(--gradient-end-color); transform: translateX(5px); }
.footer-contact { display: flex; flex-direction: column; gap: 1rem; }
.contact-item { display: flex; align-items: center; gap: 0.8rem; }
.contact-icon { width: 2.25rem; height: 2.25rem; background-color: var(--dark-bg-alt); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: var(--gradient-end-color); font-size: 0.9rem; flex-shrink: 0; border: 1px solid var(--border-color-dark);}
.contact-text { color: #9CA3AF; font-size: 0.9rem; line-height: 1.5; }
.contact-text a { color: inherit; text-decoration: none; transition: color 0.3s ease; }
.contact-text a:hover { color: var(--gradient-end-color); }
.footer-divider { height: 1px; background-color: var(--border-color-dark); margin: 3rem 0 2rem; position: relative; z-index: 1; }
.footer-bottom { display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem; position: relative; z-index: 1; }
.footer-copyright { color: #6B7280; font-size: 0.8rem; } /* Gris plus foncé pour copyright */
.footer-copyright a, .footer-copyright i { color: var(--gradient-end-color); text-decoration: none; transition: color 0.3s ease; }
.footer-copyright a:hover { filter: brightness(0.8); }
.footer-links-bottom { display: flex; gap: 1.5rem; }
.footer-links-bottom a { color: #9CA3AF; text-decoration: none; font-size: 0.8rem; transition: all 0.3s ease; }
.footer-links-bottom a:hover { color: var(--gradient-end-color); }

.logo {
  display: flex; /* Gardez ceci pour l'alignement vertical si nécessaire */
  align-items: center; /* Gardez ceci */
  text-decoration: none;
  flex-shrink: 0;
  /* margin-right: auto;  Peut-être encore utile */
  /* z-index: 1001; */
  /* Supprimez les styles spécifiques au texte comme font-weight, font-size, text-transform, text-shadow, color ici
     sauf si vous voulez qu'ils s'appliquent à un texte qui serait *à côté* de l'image, ce qui n'est pas le cas. */
}

.footer-logo {
  display: flex; /* Gardez ceci */
  align-items: center; /* Gardez ceci */
  text-decoration: none;
  /* margin-bottom: 1rem; Peut-être encore utile */
  /* Supprimez les styles de texte ici aussi */
}

/* NOUVEAUX STYLES pour l'image du logo */
.logo img,
.footer-logo img {
  max-height: 70px; /* Ajustez cette valeur ! Hauteur maximale souhaitée pour votre logo. */
  width: auto;     /* Permet à la largeur de s'ajuster pour conserver les proportions. */
  display: block;  /* Bon pour éviter des espaces inattendus sous l'image. */
  /* Vous pouvez ajouter d'autres styles si nécessaire, par exemple :
     margin-right: 10px; // Si vous aviez du texte à côté.
  */
}

/* Spécificité pour le footer si vous voulez une taille différente */
.footer-logo img {
  max-height: 70px; /* Par exemple, un peu plus grand dans le footer */
}

.logo span {
  color: var(--accent-color);
}

.footer-logo span {
  color: var(--gradient-end-color);
}

/* Animations (inchangées) */
@keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
@keyframes bounce { 0%, 20%, 50%, 80%, 100% { transform: translateY(0); } 40% { transform: translateY(-10px); } 60% { transform: translateY(-5px); } }
@keyframes rotate { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }

/* === Responsive Adjustments (inchangés structurellement) === */
/* Les ajustements de taille/mise en page restent les mêmes */
@media (max-width: 1200px) {
  .container { padding: 0 2rem; }
  .hero-content { gap: 3rem; }
  .download-container { padding: 3rem; }
  .partners-cards { gap: 1rem; }
  .partners-benefits, .partners-stats, .partners-testimonials, .partner-process, .partner-contact-form, .partner-brands { max-width: 95%; margin-top: 3rem; margin-bottom: 3rem; }
  .partner-contact-form { padding: 2.5rem; }
}

@media (max-width: 992px) {
  section { padding: 80px 0; }
  .hero { padding-top: 80px; padding-bottom: 60px; min-height: auto; text-align: center; }
  .hero-content { display: flex; flex-direction: column; gap: 0rem; align-items: center; }
  .hero-text, .hero-image, .hero-buttons { grid-column: auto; grid-row: auto; width: 100%; max-width: 600px; margin-left: auto; margin-right: auto; }
  .hero-text { order: 1; margin-bottom: 0; }
  .hero-text p { margin-bottom: 0; }
  .hero-image { max-width: 450px; margin-top: 0; order: 2; height: auto; }
  .hero-buttons { justify-content: center; margin-top: 1.5rem; order: 3; max-width: 600px; }
  header .container .header-content nav ul { display: none; }
  header .container .header-content nav .mobile-menu-button { display: inline-flex; align-items: center; justify-content: center;}
  header .container .header-content nav { display: flex; align-items: center; gap: 0.8rem; margin-left: auto; }
  header .container .header-content { display: flex; justify-content: space-between; align-items: center; flex-wrap: nowrap; }
  header .container .header-content .logo { margin-right: 0; flex-shrink: 0; }
  .download-content { gap: 2rem; }
  .download-app-preview { margin-top: 2rem; }
  .footer-content { gap: 2rem; }
  .footer-logo-area { grid-column: span 1; }
  .benefits .section-inner { padding: 3rem; }
  .partners-cards { grid-template-columns: repeat(2, 1fr); }
  .form-fields { grid-template-columns: 1fr; gap: 1rem; }
  .partner-logos { gap: 1.5rem; }
  .partner-logo { padding: 0.8rem 1.5rem; }
  .partners-benefits, .partners-stats, .partners-testimonials, .partner-process, .partner-contact-form, .partner-brands { max-width: 95%; margin-top: 2.5rem; margin-bottom: 2.5rem; }
}

@media (max-width: 768px) {
  .suggestion-item {
    padding: 0.8rem 1rem; /* Plus d'espace pour les doigts */
    min-height: 44px; /* Taille minimale recommandée pour les cibles tactiles */
  }
  
  /* Augmenter légèrement la zone cliquable */
  .map-search-button, 
  .map-location-button {
    min-width: 44px;
    min-height: 44px;
  }
  
  /* Agrandir la zone de suggestions sur petit écran */
  .search-suggestions.active {
    max-height: 250px; /* Réduire légèrement la hauteur maximale */
  }
}

@media (max-width: 768px) {
  h1 { font-size: clamp(2.5rem, 8vw, 3rem); }
  h2 { font-size: clamp(2rem, 6vw, 2.5rem); }
  .container { padding: 0 1.5rem; }
  section { padding: 60px 0; }
  .how-it-works { padding-top: 60px; }
  .hero-text p { font-size: 1.1rem; margin-bottom: 0; }
  .hero-image { max-width: 90%; }
  .hero-phone-container { max-width: 300px; }
  .hero-buttons { margin-top: 1.5rem; }
  .steps { grid-template-columns: 1fr; gap: 1.5rem; }
  .step { padding: 2rem 1.5rem; }
  .step-illustration { height: 160px; }
  .benefits-list { grid-template-columns: 1fr; gap: 1.5rem; }
  .benefit-card { padding: 1.5rem; }
  .benefit-content { padding: 1.5rem; }
  .benefits .section-inner { padding: 2rem; }
  .map-wrapper { height: 400px; }
  .pricing-options { gap: 1.5rem; }
  .pricing-card { max-width: none; }
  .download-container { padding: 2rem; }
  .download-content { grid-template-columns: 1fr; text-align: center; }
  .download-title, .download-description { text-align: center; }
  .download-buttons { justify-content: center; }
  .app-button { width: auto; justify-content: center; flex-grow: 1; max-width: 250px; }
  .footer-content { grid-template-columns: 1fr; text-align: center; }
  .footer-logo-area { max-width: none; margin: 0 auto; }
  .footer-social { justify-content: center; }
  .footer-title { padding-left: 0; text-align: center; }
  .footer-title::before { display: none; }
  .footer-links { text-align: center; }
  .footer-contact { align-items: center; }
  .contact-item { justify-content: center; }
  .footer-bottom { flex-direction: column; gap: 0.5rem; text-align: center; }
  .footer-links-bottom { justify-content: center; }
  header .container .header-content nav .btn.btn-primary { padding: 0.7rem 1.2rem; font-size: 0.9rem; }
  header .container .header-content nav .mobile-menu-button { font-size: 1.6rem; }
  header .container .header-content nav { gap: 0.5rem; }
  header .container .header-content .logo { font-size: 1.3rem; flex-shrink: 1; }
  header .container .header-content { justify-content: space-between; }
  .partners-cards { grid-template-columns: 1fr; }
  .partner-contact-form { padding: 2rem; }
  .process-steps::before { left: 1.5rem; }
  .process-step-number { width: 2.5rem; height: 2.5rem; font-size: 1.1rem; margin-right: 1rem; }
  .partner-brands h4 { font-size: 1.2rem; }
  .partner-logos { gap: 1rem; justify-content: space-around; }
  .partner-logo { padding: 0.7rem 1rem; font-size: 0.9rem; }
  .partner-logo i { font-size: 1.2rem; }
  .partners-benefits, .partners-stats, .partners-testimonials, .partner-process, .partner-contact-form, .partner-brands { margin-top: 2rem; margin-bottom: 2rem; }
}

.hero-phone.no-frame {
 border: none;
 background-color: transparent;
 box-shadow: var(--shadow-lg);
}

/* Styles pour le nouveau conteneur hero */
.hero-container {
  position: relative;
  background: linear-gradient(135deg, var(--bg-alt), #E0F2FE);
  border-radius: var(--radius-xl);
  overflow: hidden;
  padding: 3rem 2rem;
  box-shadow: var(--shadow-lg);
  border: 1px solid var(--border-color);
  margin-top: 2rem;
}

/* Ajustement pour le mode sombre */
.dark-mode .hero-container {
  background: none !important; /* Supprime tout fond existant */
  background-image: none !important; /* Force la suppression du dégradé */
  background-color: var(--dark-bg-base) !important; /* Applique uniquement la couleur noire */
}

/* Adaptation des éléments internes */
.hero-container .hero-content {
  margin: 0;
}

.hero-container .hero-text {
  padding-left: 1rem;
}

/* Ajustement du scroll-down */
.hero-container .scroll-down {
  bottom: -4rem;
}

/* Media queries */
@media (max-width: 992px) {
  .hero-container {
    padding: 2.5rem 1.5rem;
  }
  
  .hero-container .hero-text {
    padding-left: 0;
  }
}

@media (max-width: 768px) {
  .hero-container {
    padding: 2rem 1rem;
    margin-top: 2rem;
  }
}

@media (max-width: 576px) {
   h1 { font-size: 2rem; }
   h2 { font-size: 1.8rem; }
   .container { padding: 0 1rem; }
   .hero-buttons { flex-direction: column; align-items: stretch; gap: 0.8rem; }
   .hero-buttons .btn { width: 100%; }
   header .container .header-content .logo { font-size: 1.1rem; margin-right: 0.5rem; }
   header .container .header-content nav .btn.btn-primary { padding: 0.6rem 0.8rem; font-size: 0.8rem; }
   header .container .header-content nav .mobile-menu-button { font-size: 1.5rem; }
   header .container .header-content nav { gap: 0.3rem; }
   header .container .header-content { padding: 0.6rem 0; }
   .download-buttons { flex-direction: column; align-items: center; }
   .app-button { width: 100%; max-width: 280px;}
   .benefits .section-inner { padding: 1.5rem; margin-top: -10px; margin-bottom: 10px; }
   .stat-number { font-size: 2rem; }
   .stat-label { font-size: 0.8rem; }
   .testimonial-content p { font-size: 1rem; }
   .form-header h3 { font-size: 1.5rem; }
   .form-header p { font-size: 0.9rem; }
   .partner-contact-form { padding: 1.5rem; }
   .partner-logos { flex-direction: column; align-items: center; gap: 1rem; }
   .partner-logo { width: 80%; }
   .partners-benefits, .partners-stats, .partners-testimonials, .partner-process, .partner-contact-form, .partner-brands { margin-top: 1.5rem; margin-bottom: 1.5rem; }
   .partners-cards { gap: 1rem; }
   .process-steps { padding: 1.5rem; }
   .process-steps::before { left: 1rem; top: 3rem; height: calc(100% - 6rem); }
   .process-step-number { width: 2rem; height: 2rem; font-size: 1rem; }
}

/* === Desktop Scroll Down (> 992px) === */
  @media (min-width: 993px) {
     .hero-content { grid-template-columns: 1fr minmax(auto, 500px); gap: 2rem; }
     .hero-image { grid-row: 1 / 3; max-width: 500px; }
     .hero-phone-container { max-width: 400px; }
     .scroll-down { bottom: -2rem; transform: translateX(-50%) translateY(130px); }
     .scroll-down.active { transform: translateX(-50%) translateY(100px); }
}

@media (max-width: 768px) {
 .hero-phone {
   transform: translate(-50%, -50%) rotateY(-15deg) rotateX(10deg) scale(0.8);
 }
}



--- Fin du fichier : style.css ---
